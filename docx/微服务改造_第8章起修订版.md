# 微服务改造指南：�?8 章起修订�?
> 本文替换 `微服务改造_完整指南.md` 中第 8 章到�?14 章的执行口径�?>
> 修订目标：保留原 `go-zero-api` �?HTTP 响应格式、参数校验方式、JWT 中间件风格和业务错误码；同时�?`gateway` �?`sys-rpc` 的职责符合微服务边界�?
---

## �?8 �?里程�?4：以 Login 打通正确的端到端链�?
### 8.1 先明确三层职�?
从单体拆成微服务后，同一个登录接口被拆成三层�?
| �?| 所在模�?| 职责 | 可以 import | 不应�?import |
| --- | --- | --- | --- | --- |
| HTTP Handler | `gateway/internal/handler` | 解析 HTTP 请求、参数快速校验、调�?gateway logic、用 `response` 输出统一 JSON | `httpx`、`common/response`、`gateway/internal/logic` | DB model、GORM、Casbin Enforcer |
| Gateway Logic | `gateway/internal/logic` | HTTP DTO �?RPC DTO 互转、注�?HTTP 上下文、调�?`sys-rpc`、把 RPC 错误转业务错�?| `sysclient`、`sys`、`gateway/types`、`common/rpcerr` | DB model、GORM |
| RPC Logic | `service/sys/rpc/internal/logic` | 真正业务逻辑：查库、校验密码、生�?token、写登录日志、组�?RPC 响应 | model、pkg 工具、`common/xerr`、`common/jwtx` | `net/http`、`httpx`、`common/response` |

关键原则�?
- `common/response` 只在 `gateway` 使用，保证前端仍收到 `{code,msg,data}`�?- `sys-rpc` 不返�?HTTP JSON，它只返�?protobuf response �?gRPC error�?- `gateway/internal/logic` 不是废目录，它是 HTTP �?RPC 的适配层。不要把所有转发代码都塞进 handler�?- 原单体项目中 handler 里的响应处理逻辑要保留到 `gateway handler`�?- 原单体项目中 logic 里的业务逻辑要迁移到 `sys-rpc logic`，并移除 `*http.Request` 依赖�?
### 8.2 先补 RPC 错误转换�?
原单体里 handler 可以直接判断�?
```go
if codeErr, ok := err.(*xerr.CodeError); ok {
    response.Fail(w, r, codeErr.Code, codeErr.Msg)
    return
}
```

拆成 gRPC 后不能这么做。`*xerr.CodeError` 不能跨进程原样传�?gateway，必须编码成 gRPC status，再�?gateway 解出来�?
新增 `common/rpcerr/rpcerr.go`�?
```go
package rpcerr

import (
    "strconv"

    "go-zero-rpc/common/xerr"

    "google.golang.org/genproto/googleapis/rpc/errdetails"
    "google.golang.org/grpc/codes"
    "google.golang.org/grpc/status"
)

const businessReason = "BUSINESS_ERROR"

func ToStatus(err error) error {
    if err == nil {
        return nil
    }

    codeErr, ok := err.(*xerr.CodeError)
    if !ok {
        st := status.New(codes.Internal, xerr.ErrCodeMsg[xerr.ErrInternal])
        return st.Err()
    }

    grpcCode := toGrpcCode(codeErr.Code)
    st := status.New(grpcCode, codeErr.Msg)
    stWithDetails, detailErr := st.WithDetails(&errdetails.ErrorInfo{
        Reason: businessReason,
        Domain: "go-zero-rpc",
        Metadata: map[string]string{
            "code": strconv.Itoa(codeErr.Code),
        },
    })
    if detailErr != nil {
        return st.Err()
    }
    return stWithDetails.Err()
}

func FromStatus(err error) (int, string) {
    if err == nil {
        return xerr.ErrSuccess, xerr.ErrCodeMsg[xerr.ErrSuccess]
    }

    st, ok := status.FromError(err)
    if !ok {
        return xerr.ErrInternal, xerr.ErrCodeMsg[xerr.ErrInternal]
    }

    for _, detail := range st.Details() {
        info, ok := detail.(*errdetails.ErrorInfo)
        if !ok || info.Reason != businessReason {
            continue
        }
        code, convErr := strconv.Atoi(info.Metadata["code"])
        if convErr == nil {
            return code, st.Message()
        }
    }

    switch st.Code() {
    case codes.InvalidArgument:
        return xerr.ErrParamInvalid, st.Message()
    case codes.Unauthenticated:
        return xerr.ErrUnauthorized, st.Message()
    case codes.PermissionDenied:
        return xerr.ErrForbidden, st.Message()
    case codes.NotFound:
        return xerr.ErrNotFound, st.Message()
    default:
        return xerr.ErrInternal, xerr.ErrCodeMsg[xerr.ErrInternal]
    }
}

func toGrpcCode(code int) codes.Code {
    switch code {
    case xerr.ErrParamInvalid:
        return codes.InvalidArgument
    case xerr.ErrUnauthorized, xerr.ErrTokenExpired, xerr.ErrTokenInvalid:
        return codes.Unauthenticated
    case xerr.ErrForbidden:
        return codes.PermissionDenied
    case xerr.ErrNotFound, xerr.ErrUserNotFound, xerr.ErrRoleNotFound, xerr.ErrMenuNotFound:
        return codes.NotFound
    case xerr.ErrDuplicate, xerr.ErrUsernameDuplicate, xerr.ErrEmailDuplicate, xerr.ErrPhoneDuplicate, xerr.ErrRoleCodeDuplicate:
        return codes.AlreadyExists
    default:
        return codes.Internal
    }
}
```

然后�?`common/go.mod` 中通过 `go mod tidy` 补依赖�?
### 8.3 sys-rpc �?Login proto

`service/sys/rpc/pb/sys.proto` �?Login 请求必须携带 HTTP 上下文信息。因为拆分后 `sys-rpc` 拿不�?`*http.Request`�?
```protobuf
message LoginReq {
  string username = 1;
  string password = 2;
  string client_ip = 3;
  string user_agent = 4;
}

message LoginResp {
  string access_token = 1;
  string refresh_token = 2;
  int64 expires_in = 3;
  UserInfo user_info = 4;
}

service Sys {
  rpc Login(LoginReq) returns (LoginResp);
}
```

生成代码�?
```bash
cd D:/GoProject/project_new/go-zero-rpc/service/sys/rpc
goctl rpc protoc pb/sys.proto --go_out=. --go-grpc_out=. --zrpc_out=. -m
go mod tidy
```

### 8.4 迁移 sys-rpc Login logic

把原 `go-zero-api/internal/logic/auth/login_logic.go` 迁到�?
```text
service/sys/rpc/internal/logic/sys/login_logic.go
```

迁移规则�?
- `types.LoginReq` 改成 `sys.LoginReq`�?- `types.LoginResp` 改成 `sys.LoginResp`�?- `*http.Request` 参数删除�?- `middleware.GetClientIP(r)` 改成使用 `in.ClientIp`�?- `r.Header.Get("User-Agent")` 改成使用 `in.UserAgent`�?- `pkg/jwtx` 改成 `common/jwtx`�?- `pkg/constants` 改成 `common/constants`�?- `pkg/xerr` 改成 `common/xerr`�?- 返回业务错误时用 `rpcerr.ToStatus(...)` 包装�?
错误返回示例�?
```go
if err == sysmodel.ErrNotFound {
    l.recordLoginLog(in.Username, 0, in.ClientIp, in.UserAgent, 0, "用户名或密码错误")
    return nil, rpcerr.ToStatus(xerr.NewCodeError(xerr.ErrPasswordWrong))
}
```

成功返回示例�?
```go
return &sys.LoginResp{
    AccessToken: accessToken,
    RefreshToken: refreshToken,
    ExpiresIn: l.svcCtx.Config.JwtAuth.AccessExpire,
    UserInfo: &sys.UserInfo{
        UserId: user.Id,
        Username: user.Username,
        Nickname: user.Nickname,
        Email: user.Email,
        Phone: user.Phone,
        Avatar: user.Avatar,
        Roles: roleCodes,
        Menus: menuTree,
    },
}, nil
```

### 8.5 gateway.api 保留 HTTP 协议定义

`gateway/api/gateway.api` 仍然定义前端看到�?HTTP 请求/响应类型。它不是 protobuf，也不应该直接复�?RPC message�?
```go
syntax = "v1"

type LoginReq {
    Username string `json:"username"`
    Password string `json:"password"`
}

type LoginResp {
    AccessToken string `json:"accessToken"`
    RefreshToken string `json:"refreshToken"`
    ExpiresIn int64 `json:"expiresIn"`
    UserInfo UserInfo `json:"userInfo"`
}

@server (
    prefix: /api/auth
)
service gateway-api {
    @handler LoginHandler
    post /login (LoginReq) returns (LoginResp)
}
```

生成 gateway�?
```bash
cd D:/GoProject/project_new/go-zero-rpc/gateway
goctl api go -api api/gateway.api -dir . --style gozero
go mod tidy
```

### 8.6 gateway ServiceContext

`gateway/internal/config/config.go`�?
```go
package config

import (
    "github.com/zeromicro/go-zero/rest"
    "github.com/zeromicro/go-zero/zrpc"
)

type Config struct {
    rest.RestConf
    Auth struct {
        AccessSecret string
        AccessExpire int64
    }
    SysRpc zrpc.RpcClientConf
}
```

`gateway/internal/svc/service_context.go`�?
```go
package svc

import (
    "go-zero-rpc/gateway/internal/config"
    "go-zero-rpc/sys-rpc/sysclient"

    "github.com/zeromicro/go-zero/zrpc"
)

type ServiceContext struct {
    Config config.Config
    SysRpc sysclient.Sys
}

func NewServiceContext(c config.Config) *ServiceContext {
    return &ServiceContext{
        Config: c,
        SysRpc: sysclient.NewSys(zrpc.MustNewClient(c.SysRpc)),
    }
}
```

`gateway/etc/gateway-api.yaml`�?
```yaml
Name: gateway-api
Host: 0.0.0.0
Port: 8888
Mode: dev
Timeout: 30000

Auth:
  AccessSecret: "your-access-secret-key-change-in-production"
  AccessExpire: 1200

SysRpc:
  Etcd:
    Hosts:
      - 127.0.0.1:2379
    Key: sys.rpc
```

### 8.7 gateway Handler 必须使用�?response �?
原项�?Login Handler 的核心不�?`httpx.OkJsonCtx`，而是统一�?`response.OkWithData` / `response.Fail`�?
迁移后的 `gateway/internal/handler/login_handler.go` 应该保持这个形态：

```go
package handler

import (
    "net/http"

    "go-zero-rpc/common/response"
    "go-zero-rpc/common/rpcerr"
    gatewaylogic "go-zero-rpc/gateway/internal/logic"
    "go-zero-rpc/gateway/internal/svc"
    "go-zero-rpc/gateway/internal/types"

    "github.com/zeromicro/go-zero/rest/httpx"
)

func LoginHandler(svcCtx *svc.ServiceContext) http.HandlerFunc {
    return func(w http.ResponseWriter, r *http.Request) {
        var req types.LoginReq
        if err := httpx.Parse(r, &req); err != nil {
            response.FailWithMsg(w, r, err.Error())
            return
        }

        if req.Username == "" || req.Password == "" {
            response.FailWithMsg(w, r, "用户名和密码不能为空")
            return
        }

        l := gatewaylogic.NewLoginLogic(r.Context(), svcCtx)
        resp, err := l.Login(&req, r)
        if err != nil {
            code, msg := rpcerr.FromStatus(err)
            response.Fail(w, r, code, msg)
            return
        }

        response.OkWithData(w, r, resp)
    }
}
```

注意�?
- 不要�?handler 里直�?`httpx.ErrorCtx` 返回业务错误�?- 不要�?handler 里直�?`httpx.OkJsonCtx` 返回业务数据�?- 这样前端响应格式才和原项目一致�?
### 8.8 gateway Logic 负责调用 RPC �?DTO 转换

`gateway/internal/logic/login_logic.go` 不是废文件。它是适配层：

```go
package logic

import (
    "context"
    "net/http"

    "go-zero-rpc/gateway/internal/svc"
    "go-zero-rpc/gateway/internal/types"
    "go-zero-rpc/sys-rpc/sys"

    "github.com/zeromicro/go-zero/core/logx"
)

type LoginLogic struct {
    logx.Logger
    ctx context.Context
    svcCtx *svc.ServiceContext
}

func NewLoginLogic(ctx context.Context, svcCtx *svc.ServiceContext) *LoginLogic {
    return &LoginLogic{
        Logger: logx.WithContext(ctx),
        ctx: ctx,
        svcCtx: svcCtx,
    }
}

func (l *LoginLogic) Login(req *types.LoginReq, r *http.Request) (*types.LoginResp, error) {
    rpcResp, err := l.svcCtx.SysRpc.Login(l.ctx, &sys.LoginReq{
        Username: req.Username,
        Password: req.Password,
        ClientIp: getClientIP(r),
        UserAgent: r.UserAgent(),
    })
    if err != nil {
        return nil, err
    }

    return convertLoginResp(rpcResp), nil
}

func getClientIP(r *http.Request) string {
    if v := r.Header.Get("X-Forwarded-For"); v != "" {
        return v
    }
    if v := r.Header.Get("X-Real-IP"); v != "" {
        return v
    }
    return r.RemoteAddr
}

func convertLoginResp(in *sys.LoginResp) *types.LoginResp {
    if in == nil {
        return nil
    }

    return &types.LoginResp{
        AccessToken: in.AccessToken,
        RefreshToken: in.RefreshToken,
        ExpiresIn: in.ExpiresIn,
        UserInfo: convertUserInfo(in.UserInfo),
    }
}

func convertUserInfo(in *sys.UserInfo) types.UserInfo {
    if in == nil {
        return types.UserInfo{}
    }

    return types.UserInfo{
        UserId: in.UserId,
        Username: in.Username,
        Nickname: in.Nickname,
        Email: in.Email,
        Phone: in.Phone,
        Avatar: in.Avatar,
        Roles: in.Roles,
        Menus: convertMenuItems(in.Menus),
    }
}

func convertMenuItems(in []*sys.MenuItem) []types.MenuItem {
    out := make([]types.MenuItem, 0, len(in))
    for _, item := range in {
        if item == nil {
            continue
        }
        out = append(out, types.MenuItem{
            Id: item.Id,
            ParentId: item.ParentId,
            MenuName: item.MenuName,
            MenuType: item.MenuType,
            Path: item.Path,
            Component: item.Component,
            Icon: item.Icon,
            Sort: item.Sort,
            Perms: item.Perms,
            Status: int(item.Status),
            Children: convertMenuItems(item.Children),
        })
    }
    return out
}
```

### 8.9 里程�?4 验收

启动顺序�?
```bash
cd D:/GoProject/project_new/go-zero-rpc/service/sys/rpc
go run sys.go -f etc/sys.yaml

cd D:/GoProject/project_new/go-zero-rpc/gateway
go run gateway.go -f etc/gateway-api.yaml
```

登录成功响应必须是：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "accessToken": "...",
    "refreshToken": "...",
    "expiresIn": 1200,
    "userInfo": {}
  }
}
```

登录失败也必须是原业务格式，例如�?
```json
{
  "code": 10101,
  "msg": "用户名或密码错误"
}
```

---

## �?9 �?里程�?5：迁移剩�?system logic

### 9.1 迁移顺序

按原项目模块顺序迁：

1. 认证类：`RefreshToken`、`Logout`、`ChangePassword`、`GetCurrentUser`
2. 用户类：`CreateUser`、`ListUser`、`GetUser`、`UpdateUser`、`DeleteUser`、`ResetPassword`
3. 角色类：角色 CRUD、分配菜单、分配接�?4. 菜单类：菜单 CRUD、菜单树、当前用户菜�?5. 接口类：接口 CRUD、接口列�?6. 字典类：字典类型、字典数�?7. 日志类：登录日志、操作日志查询和清空
8. 文件类：文件列表、删除、注册元数据；上传见�?13 �?9. 内部方法：`CheckPermission`、`GetUserById`、`GetRolesByUserId`

### 9.2 单个接口迁移的七步法

�?`ListUser` 为例�?
**�?1 步：看原项目 handler �?logic**

原项�?handler 的职责：

- `httpx.Parse`
- 参数校验
- 调原 logic
- 根据 `xerr.CodeError` �?`response.Fail`
- 成功�?`response.OkWithData`

原项�?logic 的职责：

- �?model
- 处理业务规则
- 返回 `types.ListUserResp`
- 业务错误返回 `xerr.CodeError`

拆分后：

- handler 响应逻辑留在 gateway handler
- 业务逻辑迁到 sys-rpc logic
- DTO 转换放到 gateway logic

**�?2 步：�?sys.proto �?RPC message**

```protobuf
message ListUserReq {
  int64 page = 1;
  int64 page_size = 2;
  string keyword = 3;
  int64 status = 4;
  bool has_status = 5;
}

message UserItem {
  int64 id = 1;
  string username = 2;
  string nickname = 3;
  string email = 4;
  string phone = 5;
  int64 status = 6;
  string avatar = 7;
  string remark = 8;
  repeated string roles = 9;
  string created_at = 10;
  string updated_at = 11;
}

message ListUserResp {
  int64 total = 1;
  repeated UserItem list = 2;
}

service Sys {
  rpc ListUser(ListUserReq) returns (ListUserResp);
}
```

可选字段要显式�?`has_xxx`。例如状�?`0` 本身是有效值，不能�?`0` 判断有没有传�?
**�?3 步：重新生成 sys-rpc**

```bash
cd D:/GoProject/project_new/go-zero-rpc/service/sys/rpc
goctl rpc protoc pb/sys.proto --go_out=. --go-grpc_out=. --zrpc_out=. -m
go mod tidy
```

**�?4 步：迁移 sys-rpc logic**

```go
func (l *ListUserLogic) ListUser(in *sys.ListUserReq) (*sys.ListUserResp, error) {
    var statusPtr *int
    if in.HasStatus {
        s := int(in.Status)
        statusPtr = &s
    }

    users, total, err := l.svcCtx.SysUserModel.List(
        l.ctx,
        int(in.Page),
        int(in.PageSize),
        in.Keyword,
        statusPtr,
    )
    if err != nil {
        l.Errorf("查询用户列表失败: %v", err)
        return nil, rpcerr.ToStatus(xerr.NewCodeError(xerr.ErrInternal))
    }

    list := make([]*sys.UserItem, 0, len(users))
    for _, user := range users {
        list = append(list, &sys.UserItem{
            Id: user.Id,
            Username: user.Username,
            Nickname: user.Nickname,
            Email: user.Email,
            Phone: user.Phone,
            Status: int64(user.Status),
            Avatar: user.Avatar,
            Remark: user.Remark,
            CreatedAt: user.CreatedAt.Format("2006-01-02 15:04:05"),
            UpdatedAt: user.UpdatedAt.Format("2006-01-02 15:04:05"),
        })
    }

    return &sys.ListUserResp{
        Total: total,
        List: list,
    }, nil
}
```

**�?5 步：把原 `.api` 定义复制�?gateway**

�?`go-zero-api/api/desc/system/user.api` 里的 HTTP 路由、请求类型、响应类型，复制�?`gateway/api/system/user.api`�?
原则�?
- HTTP JSON 字段名保持不变�?- 原来�?`@server`、`middleware` 配置保留�?- 不要为了迎合 protobuf 改前端字段名�?
**�?6 步：重新生成 gateway**

```bash
cd D:/GoProject/project_new/go-zero-rpc/gateway
goctl api go -api api/gateway.api -dir . --style gozero
go mod tidy
```

**�?7 步：实现 gateway handler �?gateway logic**

handler 形态：

```go
func ListUserHandler(svcCtx *svc.ServiceContext) http.HandlerFunc {
    return func(w http.ResponseWriter, r *http.Request) {
        var req types.ListUserReq
        if err := httpx.Parse(r, &req); err != nil {
            response.FailWithMsg(w, r, err.Error())
            return
        }

        l := userlogic.NewListUserLogic(r.Context(), svcCtx)
        resp, err := l.ListUser(&req)
        if err != nil {
            code, msg := rpcerr.FromStatus(err)
            response.Fail(w, r, code, msg)
            return
        }

        response.OkWithData(w, r, resp)
    }
}
```

gateway logic 形态：

```go
func (l *ListUserLogic) ListUser(req *types.ListUserReq) (*types.ListUserResp, error) {
    rpcReq := &sys.ListUserReq{
        Page: int64(req.Page),
        PageSize: int64(req.PageSize),
        Keyword: req.Keyword,
    }
    if req.Status != nil {
        rpcReq.Status = int64(*req.Status)
        rpcReq.HasStatus = true
    }

    rpcResp, err := l.svcCtx.SysRpc.ListUser(l.ctx, rpcReq)
    if err != nil {
        return nil, err
    }

    return convertListUserResp(rpcResp), nil
}
```

### 9.3 当前用户相关接口怎么�?
原单�?logic 可能�?`r.Context()` �?userId。微服务改造后�?
- `gateway AuthMiddleware` 解析 token，把 userId 写入 HTTP context�?- gateway handler �?logic �?context �?userId�?- �?RPC 时显式放进请求字段，例如 `GetCurrentUserReq{UserId: userId}`�?- sys-rpc 不解�?HTTP token�?
示例�?
```protobuf
message GetCurrentUserReq {
  int64 user_id = 1;
}
```

gateway logic�?
```go
userId := middleware.GetUserIdFromCtx(l.ctx)
rpcResp, err := l.svcCtx.SysRpc.GetCurrentUser(l.ctx, &sys.GetCurrentUserReq{
    UserId: userId,
})
```

### 9.4 有操作者的写接口怎么�?
创建、更新、删除这类接口，如果�?logic 需要当前用户，必须把操作�?ID 显式传给 RPC�?
```protobuf
message CreateUserReq {
  string username = 1;
  string password = 2;
  int64 operator_id = 20;
}
```

不要�?sys-rpc 自己解析 HTTP token。sys-rpc 是通用后端服务，调用方将来可能不是 HTTP gateway�?
### 9.5 批量迁移时的检查点

每迁完一个接口检查：

- gateway handler 是否�?`common/response`�?- gateway logic 是否只做 RPC 转发�?DTO 转换�?- sys-rpc logic 是否没有 `net/http`、`httpx`、`response`�?- sys-rpc 业务错误是否�?`rpcerr.ToStatus`�?- gateway 是否�?`rpcerr.FromStatus` 转回业务码�?- 前端 JSON 字段名是否和原项目一致�?- 成功响应是否仍是 `{code,msg,data}`�?
---

## �?10 �?里程�?6：搭建完�?Gateway

### 10.1 gateway.api 的组织方�?
推荐结构�?
```text
gateway/api/gateway.api
gateway/api/auth.api
gateway/api/system/user.api
gateway/api/system/role.api
gateway/api/system/menu.api
gateway/api/system/api.api
gateway/api/system/dict.api
gateway/api/system/log.api
gateway/api/system/file.api
```

`gateway/api/gateway.api`�?
```go
syntax = "v1"

import (
    "auth.api"
    "system/user.api"
    "system/role.api"
    "system/menu.api"
    "system/api.api"
    "system/dict.api"
    "system/log.api"
    "system/file.api"
)
```

子文件直接从�?`go-zero-api/api/desc/...` 复制。HTTP 层协议不要因为拆微服务而改变�?
### 10.2 handler �?logic 的使用规�?
goctl 会生成两类文件：

```text
gateway/internal/handler/xxx_handler.go
gateway/internal/logic/xxx_logic.go
```

正确使用方式�?
- handler 保留原项目的响应封装方式�?- logic 实现 RPC 调用�?DTO 转换�?- 不要�?handler 直接写大�?RPC 字段映射�?- 不要�?gateway logic 访问数据库�?
### 10.3 统一响应与错误处�?
�?gateway 里可以加一个小工具，减少每�?handler 重复写：

```go
package handler

import (
    "net/http"

    "go-zero-rpc/common/response"
    "go-zero-rpc/common/rpcerr"
)

func writeRpcError(w http.ResponseWriter, r *http.Request, err error) {
    code, msg := rpcerr.FromStatus(err)
    response.Fail(w, r, code, msg)
}
```

handler 中统一使用�?
```go
if err != nil {
    writeRpcError(w, r, err)
    return
}
```

### 10.4 ServiceContext 中间件字�?
�?`.api` 中如果有�?
```go
@server (
    middleware: AuthMiddleware,CasbinMiddleware
)
```

�?`gateway/internal/svc/service_context.go` 必须有同名字段：

```go
type ServiceContext struct {
    Config config.Config
    SysRpc sysclient.Sys
    AuthMiddleware rest.Middleware
    CasbinMiddleware rest.Middleware
}
```

字段名必须和 `.api` 里的 middleware 名字一致�?
---

## �?11 �?里程�?7：迁移中间件

### 11.1 AuthMiddleware 留在 gateway

�?`go-zero-api/internal/middleware/auth_middleware.go` 的职责是 HTTP token 认证，所以迁到：

```text
gateway/internal/middleware/auth_middleware.go
```

修改 import�?
```go
go-zero-admin/pkg/jwtx      -> go-zero-rpc/common/jwtx
go-zero-admin/pkg/response  -> go-zero-rpc/common/response
go-zero-admin/internal/config -> go-zero-rpc/gateway/internal/config
```

职责保持不变�?
- �?`Authorization` �?Bearer token�?- �?`common/jwtx` 解析�?- userId、username 写入 context�?- 失败时用 `response.FailUnauthorized` �?`response.Fail`�?
### 11.2 CasbinMiddleware 不再直连数据�?
原单�?Casbin 中间件会�?
1. �?context �?userId�?2. �?`sys_user_role`�?3. �?`sys_role`�?4. 用本�?Enforcer 判断权限�?
拆分�?gateway 不应该直连数据库，也不应该持�?Casbin Enforcer。正确做法：

```text
gateway CasbinMiddleware
  -> �?context �?userId、HTTP path、method
  -> �?sys-rpc.CheckPermission
  -> 根据 allowed 返回放行�?response.FailForbidden

sys-rpc CheckPermissionLogic
  -> 查用户角�?  -> �?Enforcer 判断角色是否有权�?  -> 返回 allowed
```

proto�?
```protobuf
message CheckPermissionReq {
  int64 user_id = 1;
  string path = 2;
  string method = 3;
}

message CheckPermissionResp {
  bool allowed = 1;
}

service Sys {
  rpc CheckPermission(CheckPermissionReq) returns (CheckPermissionResp);
}
```

gateway middleware�?
```go
func NewCasbinMiddleware(rpc sysclient.Sys) rest.Middleware {
    return func(next http.HandlerFunc) http.HandlerFunc {
        return func(w http.ResponseWriter, r *http.Request) {
            userId := GetUserIdFromCtx(r.Context())
            if userId == 0 {
                response.FailUnauthorized(w, r)
                return
            }

            resp, err := rpc.CheckPermission(r.Context(), &sys.CheckPermissionReq{
                UserId: userId,
                Path: r.URL.Path,
                Method: r.Method,
            })
            if err != nil {
                code, msg := rpcerr.FromStatus(err)
                response.Fail(w, r, code, msg)
                return
            }
            if !resp.Allowed {
                response.FailForbidden(w, r)
                return
            }

            next.ServeHTTP(w, r)
        }
    }
}
```

ServiceContext 注册�?
```go
func NewServiceContext(c config.Config) *ServiceContext {
    sysRpc := sysclient.NewSys(zrpc.MustNewClient(c.SysRpc))
    return &ServiceContext{
        Config: c,
        SysRpc: sysRpc,
        AuthMiddleware: middleware.AuthMiddleware(c),
        CasbinMiddleware: middleware.NewCasbinMiddleware(sysRpc),
    }
}
```

### 11.3 CheckPermissionLogic 放在 sys-rpc

`sys-rpc` 里保�?Enforcer �?model�?
```go
func (l *CheckPermissionLogic) CheckPermission(in *sys.CheckPermissionReq) (*sys.CheckPermissionResp, error) {
    roleIds, err := l.svcCtx.SysUserRoleModel.GetRoleIdsByUserId(l.ctx, in.UserId)
    if err != nil {
        return nil, rpcerr.ToStatus(xerr.NewCodeError(xerr.ErrInternal))
    }

    for _, roleId := range roleIds {
        role, err := l.svcCtx.SysRoleModel.FindOneByRoleId(l.ctx, roleId)
        if err != nil || role == nil || role.Status != 1 {
            continue
        }

        allowed, err := l.svcCtx.Enforcer.Enforce(role.Code, in.Path, in.Method)
        if err == nil && allowed {
            return &sys.CheckPermissionResp{Allowed: true}, nil
        }
    }

    return &sys.CheckPermissionResp{Allowed: false}, nil
}
```

### 11.4 中间件迁移验�?
- 未登录访问受保护接口：返�?`{code:10002,msg:"未授权，请先登录"}`�?- token 过期：返回原项目 token 过期业务码�?- 登录但无权限：返�?`{code:10003,msg:"权限不足，无法访�?}`�?- 有权限：正常进入 handler�?- gateway 不应出现数据库连接配置�?
---

## �?12 �?里程�?8：操作日志迁�?
### 12.1 操作日志留在 gateway 捕获，写库交�?sys-rpc

�?`OperLogMiddleware` 依赖 HTTP 信息�?
- URL
- HTTP method
- 请求�?- 响应�?- user-agent
- client IP

这些信息只有 gateway 完整拥有。sys-rpc 只能看到 RPC method �?protobuf 参数，不适合直接�?gRPC interceptor 还原原来的操作日志�?
正确链路�?
```text
gateway OperLogMiddleware
  -> 包装 ResponseWriter 捕获响应 JSON
  -> 读取并恢�?r.Body
  -> next.ServeHTTP
  -> 异步调用 sys-rpc.WriteOperLog

sys-rpc WriteOperLogLogic
  -> �?sys_oper_log �?```

### 12.2 sys.proto

```protobuf
message WriteOperLogReq {
  string title = 1;
  string oper_type = 2;
  string method = 3;
  string req_method = 4;
  int64 oper_user_id = 5;
  string oper_name = 6;
  string oper_url = 7;
  string oper_ip = 8;
  string user_agent = 9;
  string req_param = 10;
  string json_result = 11;
  int64 status = 12;
  string error_msg = 13;
  int64 cost_time_ms = 14;
}

message WriteOperLogResp {
  int64 id = 1;
}

service Sys {
  rpc WriteOperLog(WriteOperLogReq) returns (WriteOperLogResp);
}
```

### 12.3 注意响应体格�?
因为 gateway handler 已统一使用 `common/response`，操作日志捕获到的响应体会是�?
```json
{"code":0,"msg":"success","data":{...}}
```

这才和原项目一致。若 handler 直接�?`httpx.OkJsonCtx`，日志里的响应结构也会变，前端和日志都会不一致�?
---

## �?13 �?里程�?9：文件上�?
### 13.1 文件流不穿过 sys-rpc

上传接口是例外。不要把 multipart 文件内容塞进 gRPC�?
正确链路�?
```text
browser multipart/form-data
  -> gateway UploadFileHandler
  -> 校验文件类型和大�?  -> 保存到本地、NFS、MinIO �?OSS
  -> �?sys-rpc.RegisterFile �?sys_file 元数�?  -> response.OkWithData 返回文件信息
```

### 13.2 upload 包放 gateway

�?`pkg/upload` �?HTTP 文件上传工具，迁到：

```text
gateway/pkg/upload
```

或如果你希望多个 HTTP 网关复用，也可以放：

```text
common/upload
```

但不要放�?`sys-rpc/pkg/upload`，除�?sys-rpc 真的直接处理文件流�?
### 13.3 sys-rpc 只写元数�?
```protobuf
message RegisterFileReq {
  string file_name = 1;
  string file_url = 2;
  int64 file_size = 3;
  string mime_type = 4;
  int64 uploader_id = 5;
}

message RegisterFileResp {
  int64 file_id = 1;
}
```

---

## �?14 �?端到端联调与验收

### 14.1 编译检�?
```bash
cd D:/GoProject/project_new/go-zero-rpc/common
go test ./...

cd D:/GoProject/project_new/go-zero-rpc/service/sys/rpc
go test ./...

cd D:/GoProject/project_new/go-zero-rpc/gateway
go test ./...
```

### 14.2 启动顺序

```bash
# 1. etcd
docker start etcd-dev

# 2. sys-rpc
cd D:/GoProject/project_new/go-zero-rpc/service/sys/rpc
go run sys.go -f etc/sys.yaml

# 3. gateway
cd D:/GoProject/project_new/go-zero-rpc/gateway
go run gateway.go -f etc/gateway-api.yaml
```

### 14.3 必测响应格式

所�?HTTP 接口都必须返回统一格式�?
成功�?
```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

失败�?
```json
{
  "code": 10001,
  "msg": "参数错误"
}
```

不允许出现：

- 直接裸返回业务对象�?- go-zero 默认错误格式�?- gRPC 原始错误字符串暴露给前端�?
### 14.4 回归测试清单

- [ ] `POST /api/auth/login` 成功返回 token，响应包裹在 `data` 里�?- [ ] 登录密码错误返回原业务码 `10101`�?- [ ] 参数错误返回原业务码 `10001`�?- [ ] 未登录访问受保护接口返回 `10002`�?- [ ] 无权限访问返�?`10003`�?- [ ] `GetCurrentUser` �?gateway context �?userId，并显式传给 sys-rpc�?- [ ] 用户、角色、菜单、接口、字典、日志接口响应字段与原项目一致�?- [ ] 操作日志记录的是 gateway �?HTTP URL、method、请求体、响应体�?- [ ] gateway 没有数据库连接�?- [ ] sys-rpc 没有 `net/http`、`httpx`、`common/response`�?
### 14.5 判断设计是否正确

满足下面条件，才算真正符合微服务设计�?
- gateway �?HTTP 协议适配层，不承载数据库业务�?- sys-rpc 是系统业务能力服务，不处�?HTTP 响应�?- common 只放跨模块稳定依赖：`response`、`xerr`、`rpcerr`、`jwtx`、`constants`�?- 所有跨服务调用显式传参，不依赖 HTTP context�?- 前端协议没有因为拆微服务而改变�?
