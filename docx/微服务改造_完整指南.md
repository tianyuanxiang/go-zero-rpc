# 微服务改造完整指南（从零到上线）

> 适用对象：从未写过微服务的开发�?> 目标：把当前 `go-zero-admin`（HTTP 单体）改造为 `sys-rpc`（gRPC 微服务）+ `gateway`（HTTP 网关），后续可平滑接入业务微服务（如 `plating-rpc`）�?> 改造哲学：**先跑通，再扩展；功能不简化，代码不删减�?*

---

## 目录

- [�?0 �?改造前你必须先理解的事](#�?0-�?改造前你必须先理解的事)
- [�?1 �?目标架构总览](#�?1-�?目标架构总览)
- [�?2 �?核心概念扫盲（不懂这些就别动手）](#�?2-�?核心概念扫盲不懂这些就别动手)
- [�?3 �?准备工作（环境、工具、依赖）](#�?3-�?准备工作环境工具依赖)
- [�?4 �?改造路线图�?0 个里程碑）](#�?4-�?改造路线图10-个里程碑)
- [�?5 �?里程�?1：创建项目骨架](#�?5-�?里程�?1创建项目骨架)
- [�?6 �?里程�?2：编�?sys.proto 并生�?RPC 骨架](#�?6-�?里程�?2编写-sysproto-并生�?rpc-骨架)
- [�?7 �?里程�?3：迁�?model �?+ pkg 工具包](#�?7-�?里程�?3迁移-model-�?-pkg-工具�?
- [�?8 �?里程�?4：迁�?logic（以 Login 为例打通端到端）](#�?8-�?里程�?4迁移-logic�?login-为例打通端到端)
- [�?9 �?里程�?5：迁移剩�?system logic](#�?9-�?里程�?5迁移剩余-system-logic)
- [�?10 �?里程�?6：搭�?Gateway](#�?10-�?里程�?6搭建-gateway)
- [�?11 �?里程�?7：迁移中间件（Auth + Casbin）](#�?11-�?里程�?7迁移中间件auth--casbin)
- [�?12 �?里程�?8：操作日志的迁移（最容易踩坑的一步）](#�?12-�?里程�?8操作日志的迁移最容易踩坑的一�?
- [�?13 �?里程�?9：文件上传的特殊处理](#�?13-�?里程�?9文件上传的特殊处�?
- [�?14 �?里程�?10：端到端联调与验收](#�?14-�?里程�?10端到端联调与验收)
- [�?15 �?未来如何嵌入业务服务（以 plating-rpc 为例）](#�?15-�?未来如何嵌入业务服务�?plating-rpc-为例)
- [�?16 �?常见坑与排查方法](#�?16-�?常见坑与排查方法)
- [附录 A：完�?sys.proto 模板](#附录-a完整-sysproto-模板)
- [附录 B：检查清单（验收标准）](#附录-b检查清单验收标�?

---

## �?0 �?改造前你必须先理解的事

### 0.1 为什么要改造？说人�?
| 现状（单体）                               | 改造后（微服务�?                                 |
| ------------------------------------------ | ------------------------------------------------- |
| 一个进程，一个二进制，HTTP 直接接数据库    | 多个进程，Gateway �?HTTP，sys-rpc 接数据库       |
| 加新业务（如电镀业务）只能往同一个项目塞   | 加新业务 = 新建一�?RPC 服务，独立部署、独立扩缩容 |
| 系统管理逻辑无法被其他项目复�?            | sys-rpc 可作为基础服务被任何业务服务以 gRPC 调用  |

一句话�?*现在你写�?通用系统管理"代码会变成一�?基础�?——任何新业务项目，启动一�?plating-rpc / order-rpc / xxx-rpc，通过 gRPC �?sys-rpc 拿用户、查权限、记操作日志，不用再 copy 代码�?*

### 0.2 改造心智准备（最重要�?
- **改�?�?重写**。你 90% �?logic 代码会原封不动搬过去，只�?import 路径和类型名�?- **不要一次性全部迁�?*。先�?1 个接口（比如 Login），跑通完整链路（前端 �?Gateway �?sys-rpc �?MySQL �?返回），再批量迁其他的�?- **不要一开始就追求完美**。先让流程跑起来，再�?Casbin、再�?OperLog、再补文件上传�?- **保留原项目能�?*。新项目放在 `D:\GoProject\project_new\go-zero-admin\`（新目录），改造期间老项�?`go-zero-rpc\` 不要动�?
### 0.3 一�?听起来很�?的概念其实很简�?
- **gRPC 调用** = 函数调用，只是这个函数实现在另一个进程里，参数通过网络传过去。你�?`svcCtx.SysRpc.Login(ctx, &sys.LoginReq{...})` 跟你现在�?`svcCtx.SysUserModel.FindOneByUsername(ctx, name)` 一样自然�?- **Protobuf** = 跨进程版�?Go struct。`.proto` 文件就是 struct 定义文件，`goctl` 帮你把它编译成各语言可用的代码�?- **etcd** = 一个微型数据库，专门用来存"哪个服务在哪�?IP 端口"。你只需�?`docker run` 一下，剩下都是 go-zero 自动跟它打交道�?
---

## �?1 �?目标架构总览

### 1.1 改造前（你当前的样子）

```
浏览�?   �? HTTP (8888)
   �?┌──────────────────────────────────────────────────�?�? go-zero-admin (单体进程)                        �?�? ┌────────────────────────────────────────────�? �?�? �?handler/auth/login_handler.go              �? �?�? �?  └─> logic/auth/login_logic.go            �? �?�? �?        └─> svcCtx.SysUserModel (函数调用)  �? �?�? �?              └─> MySQL                    �? �?�? �?middleware/AuthMiddleware                  �? �?�? �?middleware/CasbinMiddleware                �? �?�? �?middleware/OperLogMiddleware               �? �?�? └────────────────────────────────────────────�? �?└──────────────────────────────────────────────────�?```

### 1.2 改造后（最终目标）

```
浏览�?   �? HTTP (8888)
   �?┌─────────────────────────�?�? gateway (HTTP 进程)     �?�? - JWT 认证中间�?        �?�? - Casbin 鉴权中间�?     �?�? - 参数解析、JSON 响应     �?�? - �?HTTP 翻译�?gRPC    �?└─────────────────────────�?   �? gRPC (9100)            �? gRPC (9200)
   �?                         �?┌─────────────────────�?   ┌─────────────────────────�?�? sys-rpc (9100)      �?   �? plating-rpc (9200)      �? <-- 未来扩展
�? - 用户/角色/菜单     �?   �? - 业务逻辑             �?�? - 字典/接口/文件     �?   �? - �?sys-rpc 拿用户信�?�?�? - 登录/操作日志      �?   �?                        �?└─────────────────────�?   └─────────────────────────�?   �? GORM/sqlx                �? GORM/sqlx
   �?                           �?┌──────────────�?              ┌──────────────�?�? MySQL       �?              �? MySQL       �?�? (sys_* �?  �?              �? (plate_* �? �?└──────────────�?              └──────────────�?
       ┌──────────────────────�?       �? etcd (2379)         �? <-- 服务注册中心，所�?RPC 启动时把自己注册到这�?       └──────────────────────�?```

### 1.3 一次完整请求的"经过"

�?前端创建用户"为例�?
```
1. 前端 POST /api/system/user  Body: {"username":"zs","password":"xxx",...}
                                Header: Authorization: Bearer <token>
                                          �?2. gateway 收到 HTTP 请求
   ├─ AuthMiddleware: 验证 JWT �?提取 userId 写入 ctx
   ├─ CasbinMiddleware: �?userId + path + method �?sys-rpc.CheckPermission
   �?                   返回 false 时直�?403
   └─ handler: httpx.Parse(req) �?�?svcCtx.SysRpc.CreateUser(ctx, &sys.CreateUserReq{...})
                                          �?                                  gRPC 跨进程调�?                                          �?3. sys-rpc 收到 gRPC 请求
   ├─ logic.CreateUser:
   �?  ├─ svcCtx.SysUserModel.Insert(...)      <-- 这部分代码跟单体时代一模一�?   �?  ├─ svcCtx.SysUserRoleModel.Insert(...)
   �?  └─ 返回 &sys.CommonResp{Code:0, Msg:"ok"}
   └─ 通过 gRPC 把响应返�?gateway
                                          �?4. gateway �?RPC 响应�?JSON 返给前端
```

**前端完全无感�?*——URL 还是 `http://xxx:8888/api/system/user`�?
---

## �?2 �?核心概念扫盲（不懂这些就别动手）

### 2.1 gRPC vs HTTP

| 项目         | HTTP                       | gRPC                                   |
| ------------ | -------------------------- | -------------------------------------- |
| 数据格式     | JSON（文本）               | Protobuf（二进制，更小更快）            |
| 接口定义     | `.api` 文件                | `.proto` 文件                          |
| 客户端写�?  | `httpx.Parse(r, &req)`     | `client.Method(ctx, &Req{...})`        |
| 用�?        | 浏览�?�?服务�?            | 服务 �?服务（内部通信�?               |

**对你而言关键的是**：写 `.proto` 文件 + `goctl rpc protoc` 生成代码，剩下跟�?go-zero HTTP 几乎一样�?
### 2.2 Protobuf 文件长什么样

对比你现在的 `.api`�?
```go
// user.api
type ListUserReq {
    Page     int    `form:"page,default=1"`
    PageSize int    `form:"pageSize,default=20"`
    Keyword  string `form:"keyword,optional"`
    Status   *int   `form:"status,optional"`
}
```

对应�?`.proto`�?
```protobuf
// sys.proto
message ListUserReq {
    int64  page     = 1;     // 数字 1/2/3 是字段编号（不是值），传输时按编号编�?    int64  pageSize = 2;
    string keyword  = 3;
    int64  status   = 4;     // 注意：proto3 没有指针，改�?0 表示"未设�?
    bool   hasStatus = 5;    // 如果 status=0 也是一种过滤值，用一个额外字段标记是否传�?}
```

**关键差异（必须记住）**�?
1. proto3 **没有原生的指�?可选语�?*。你 `.api` 里的 `*int` �?proto 里要么用 0 表示空，要么加一�?`bool hasXxx` 标记，要么用 `optional`（proto3 后期版本支持，但生成�?Go 代码用法更复杂）�?*推荐方案**：用 0/空字符串表示未传，业务上能区分的字段（如 status �?0 是有效值）就加 `bool hasStatus`�?2. proto 字段名风格是 `snake_case`，但 protoc-gen-go 会自动转�?Go �?`CamelCase`�?3. 字段编号 1-15 �?1 字节编码�?6+ �?2 字节�?*高频字段编号尽量�?*�?
### 2.3 etcd 是什�?
把它想象成一�?电话�?�?
- sys-rpc 启动时打电话�?etcd�?喂，我叫 sys.rpc，我�?192.168.1.10:9100"
- gateway 启动时打电话�?etcd�?sys.rpc 在哪�?
- etcd 答："�?192.168.1.10:9100"
- gateway 拿到地址后跟 sys-rpc �?gRPC 连接

**为什么不写死 IP�?* 因为 sys-rpc 可以部署多个实例（负载均衡），etcd 会把所有实例的地址都告�?gateway，gateway �?round-robin 轮询调用�?
### 2.4 goctl 是什�?
它是 go-zero �?代码生成�?。你�?`.proto`，它帮你生成�?
```
sys-rpc/
├── etc/sys.yaml                    <-- 配置文件骨架
├── internal/
�?  ├── config/config.go            <-- 配置 struct
�?  ├── server/sys_server.go        <-- gRPC 服务端入口（你不用改�?�?  ├── svc/service_context.go      <-- ServiceContext（你要改这个�?�?  └── logic/                      <-- 业务逻辑文件（你要填这里�?�?      ├── login_logic.go
�?      ├── list_user_logic.go
�?      └── ...
├── sys/                            <-- protobuf 生成�?Go 类型
�?  ├── sys.pb.go
�?  └── sys_grpc.pb.go
├── sysclient/                      <-- 给别人调用的客户端代�?�?  └── sys.go
└── sys.go                          <-- main 入口
```

**你只需要改 4 个地�?*：`.proto` 文件、`service_context.go`、`logic/*.go`、`etc/*.yaml`。其余都是自动生成�?
---

## �?3 �?准备工作（环境、工具、依赖）

### 3.1 必装的工具（按顺序操作）

#### 3.1.1 安装 protoc 编译�?
```bash
# Windows: 下载 protoc-xx.x-win64.zip
# https://github.com/protocolbuffers/protobuf/releases
# 解压后把 bin/ 目录加入 PATH

# 验证
protoc --version
# 输出：libprotoc 25.x
```

#### 3.1.2 安装 protoc-gen-go �?protoc-gen-go-grpc

```bash
go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

# 验证（这俩二进制会安装到 $GOPATH/bin�?protoc-gen-go --version
protoc-gen-go-grpc --version
```

#### 3.1.3 升级 goctl（你已经�?1.9.2，够用了�?
```bash
goctl --version
# 1.9.2 ok。如果太旧：
go install github.com/zeromicro/go-zero/tools/goctl@latest
```

#### 3.1.4 启动 etcd（用 Docker，最简单）

```bash
docker run -d \
  --name etcd-dev \
  -p 2379:2379 \
  -p 2380:2380 \
  -e ALLOW_NONE_AUTHENTICATION=yes \
  -e ETCD_ADVERTISE_CLIENT_URLS=http://0.0.0.0:2379 \
  bitnami/etcd:latest

# 验证
docker exec etcd-dev etcdctl put foo bar
docker exec etcd-dev etcdctl get foo
# 输出：foo \n bar
```

如果没装 Docker：从 https://etcd.io/docs/v3.5/install/ 下载 Windows 版二进制，解压后双击 `etcd.exe`�?
### 3.2 关于复用 pkg 的策�?
你现在的 `pkg/` 目录里这些工具，**90% 改造后还能直接�?*�?
| 包名         | 用�?                      | 改造方�?                                     |
| ------------ | -------------------------- | --------------------------------------------- |
| `casbin/`    | Casbin Enforcer 初始�?    | 复制�?sys-rpc                                |
| `constants/` | 常量（角色编码等�?        | 复制�?common（gateway 和所�?rpc 都要用）    |
| `dict/`      | 字典工具                   | 复制�?sys-rpc                                |
| `encrypt/`   | bcrypt 密码加密            | 复制�?sys-rpc                                |
| `excel/`     | Excel 导出                 | 复制�?sys-rpc                                |
| `jwtx/`      | JWT 生成/解析              | **复制�?common**（gateway 验证 token，sys-rpc 生成 token�?|
| `orm/`       | GORM 初始�?               | 复制�?sys-rpc                                |
| `response/`  | HTTP 响应封装              | 复制�?gateway（rpc 不直接处�?HTTP�?        |
| `sqlx/`      | sqlx 超时包装�?           | 复制�?sys-rpc                                |
| `upload/`    | 文件上传                   | 复制�?sys-rpc（也可以单独做文件服务）         |
| `xerr/`      | 业务错误�?                | **复制�?common**（rpc �?status.Errorf 包装，gateway 解出来转 HTTP code�?|

---

## �?4 �?改造路线图�?0 个里程碑�?
按这个顺序做，每完成一个里程碑�?commit 一次，能保证随时回滚：

| 里程�?| 内容                                                | 预估工时 | 验收标准                              |
| :----: | --------------------------------------------------- | :------: | ------------------------------------- |
|   1    | 创建新项目目录结构、初始化 go.mod                   |  0.5h    | `go mod tidy` 通过                    |
|   2    | �?`sys.proto` + `goctl` 生成 sys-rpc 骨架          |   3h     | sys-rpc �?`go run` 启动监听 9100     |
|   3    | 迁移 model + pkg �?sys-rpc                         |   1h     | `go build ./...` 通过                 |
|   4    | 迁移 Login logic + 写最简 gateway 跑�?Login        |   3h     | 前端能用 admin/admin123 登录成功      |
|   5    | 迁移其他 system logic（用�?角色/菜单/字典/接口/日志/文件�?| 6-8h | 所有原 HTTP 接口在新系统能跑          |
|   6    | gateway 完整接入所有路�?                           |   3h     | 路由表与原项目完全一�?               |
|   7    | 迁移 AuthMiddleware + CasbinMiddleware              |   2h     | 鉴权失败返回 401/403                  |
|   8    | 迁移 OperLogMiddleware（改�?gRPC interceptor�?    |   2h     | 操作日志能写�?sys_oper_log           |
|   9    | 文件上传特殊处理（gateway 直收 multipart�?         |   2h     | 文件能上传、下�?                     |
|   10   | 全量回归测试 + 部署文档                             |   4h     | 所有原 HTTP 接口在新系统通过          |

**总工时约 26-30h**，按每天投入 4h 计算，约 7-10 天�?
---

## �?5 �?里程�?1：创建项目骨�?
> **本章重要原则（先读完再动手）**
>
> 1. **原项�?`D:\GoProject\project_new\go-zero-rpc\` 完全不动**——既不改、也不删。它是您�?备份"�?参�?，改造期间随时回去看代码�?> 2. **新项目放�?`D:\GoProject\project_new\go-zero-admin\`**——是另一个独立目录，跟原项目并列�?> 3. **Go Module 概念预热**：每�?Go 模块都需要一�?`go.mod` 文件来声�?这个目录是个独立模块、它的名字是 X、它依赖了哪些第三方�?。`go.mod` 不是凭空出现的，�?`go mod init <模块�?` 命令产生的�?> 4. **依赖下载 �?模块声明**：`go mod init` 只创建空�?`go.mod`（没有任�?require）；要让 `go.mod` 自动收集所�?import 进来的第三方库，需要执�?`go mod tidy`，它会扫�?`.go` 文件里的 import，下载到本地、写�?`go.mod` �?`go.sum`�?>
> **本章结束后，您将拥有 3 个独立模块（common、sys-rpc、gateway�? 1 �?go.work 把它们串起来，且每个模块都能 `go build ./...` 通过�?*

---

### 5.1 第一步：建立项目根目�?
#### 5.1.1 命令

打开终端（推�?Git Bash �?PowerShell），执行�?
```bash
mkdir D:/GoProject/project_new/go-zero-admin
cd D:/GoProject/project_new/go-zero-admin
```

#### 5.1.2 这两行命令做什�?
- `mkdir` 创建一个空目录
- `cd` 把终端当前位置切到这个新目录

#### 5.1.3 产物

```
D:\GoProject\project_new\
├── go-zero-rpc\          <-- 原项目（不要动！�?└── go-zero-admin\        <-- 新建空目录（本次的工作区�?```

#### 5.1.4 原项目处�?
不删、不改、不动。整个改造期间它就放着�?
#### 5.1.5 验证

```bash
pwd
# 应该输出�?d/GoProject/project_new/go-zero-admin（Git Bash�?# 或：D:\GoProject\project_new\go-zero-admin（PowerShell�?```

---

### 5.2 第二步：创建子目录骨�?
#### 5.2.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin

mkdir common
mkdir -p service/sys/rpc
mkdir gateway
mkdir etc
mkdir deploy
```

> Windows �?`mkdir` 不支�?`-p`，需要用 Git Bash �?PowerShell�?> 如果�?cmd，改成：`mkdir service\sys\rpc`（cmd 会自动创建多层）�?
#### 5.2.2 这些命令做什�?
一次性把"以后要往里塞代码的几个空文件�?建出来，避免后面手忙脚乱�?
#### 5.2.3 产物

```
D:\GoProject\project_new\go-zero-admin\
├── common\               <-- 跨服务共享代码（jwtx、xerr、constants�?├── service\
�?  └── sys\
�?      └── rpc\          <-- sys-rpc 服务（goctl 后续生成代码�?├── gateway\              <-- HTTP 网关（goctl 后续生成代码�?├── etc\                  <-- 全局配置（如 rbac_model.conf�?└── deploy\               <-- 部署脚本（docker-compose.yml�?```

目前每个文件夹都是空的——这是正常的�?
---

### 5.3 第三步：初始�?common 模块（最关键，最容易卡住�?
> **新手必读：什么是 `go mod init`？什么是 `go.mod`�?*
>
> Go �?模块"管理一个或多个 Go 包。一个模块对应一�?`go.mod` 文件（由 `go mod init <模块�?` 命令产生）�?> `go.mod` 文件的作用是声明�?> - 这个模块叫什么名字（其他代码 import 时用这个名字�?> - 用什么版本的 Go
> - 依赖了哪些第三方库（require 段）
>
> **没有 `go.mod`，IDE 就不认这个目录是 Go 模块**，所�?import 全部报红——这就是您遇到的"Cannot resolve symbol"的根本原因�?
#### 5.3.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/common

go mod init go-zero-admin/common
```

#### 5.3.2 这两行命令做什�?
- `cd` 进入 `common` 目录
- `go mod init go-zero-admin/common`
  - 在当前目录生成一�?`go.mod` 文件
  - 把模块名注册�?`go-zero-admin/common`
  - 以后任何 Go 代码 `import "go-zero-admin/common/jwtx"` 都会指向这个模块下的 `jwtx/` 子目�?
#### 5.3.3 产物

`common/go.mod` 文件被创建出来，内容大约是：

```
module go-zero-admin/common

go 1.24.0
```

只有两行——这�?*空模�?*的样子。还没有任何 `require`（因为还没复制任何代码进来）�?
#### 5.3.4 验证

```bash
ls common/
# 应该输出：go.mod
```

```bash
cat common/go.mod
# 应该输出上面那两�?```

---

### 5.4 第四步：�?jwtx / xerr / constants 复制�?common

> **新手必读：原文件要不要删�?*
>
> **不要删�?* 原项�?`D:\GoProject\project_new\go-zero-rpc\pkg\` 完整保留——它�?参考资�?+ 回滚备份"�?> 我们做的�?复制"——原文件留在原地，新项目用一份副本。改造完成、新项目稳定运行后，再决定是否归档原项目�?
#### 5.4.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin

cp -r ../go-zero-rpc/pkg/jwtx       common/
cp -r ../go-zero-rpc/pkg/xerr       common/
cp -r ../go-zero-rpc/pkg/constants  common/
```

> �?cmd 里改成：`xcopy /E /I ..\go-zero-rpc\pkg\jwtx common\jwtx`
> 或者在文件管理器里直接 Ctrl+C / Ctrl+V，效果一样�?
#### 5.4.2 这些命令做什�?
- 把原项目的三个目录及其文件原样拷贝到新项目的 `common/` �?- `-r` 表示递归（包括子目录和所有文件）

#### 5.4.3 产物

```
D:\GoProject\project_new\go-zero-admin\common\
├── go.mod                   <-- �?5.3 步产�?├── jwtx\
�?  └── jwt.go               <-- 复制过来�?├── xerr\
�?  └── errcode.go           <-- 复制过来�?└── constants\
    └── role.go              <-- 复制过来�?```

#### 5.4.4 原文件处�?
**原项�?`D:\GoProject\project_new\go-zero-rpc\pkg\jwtx\`、`xerr\`、`constants\` 全部保留不动�?* 您看到的 `D:\GoProject\project_new\go-zero-admin\common\jwtx\jwt.go` 是新项目的副本，跟原文件没有任何关联——改新项目不会影响原项目�?
#### 5.4.5 验证

```bash
ls common/jwtx/
# 输出：jwt.go

ls common/xerr/
# 输出：errcode.go

ls common/constants/
# 输出：role.go
```

---

### 5.5 第五步：修复 import 路径（这一步漏了，IDE 必报红）

复制过来的文件里�?import 路径还指�?原模块名"，要改成"新模块名"�?
#### 5.5.1 当前状态（打开 `common/jwtx/jwt.go`�?
```go
package jwtx

import (
    "errors"
    "time"

    "github.com/golang-jwt/jwt/v5"   // <-- 第三方依赖，路径不需要改
)
```

`jwt.go` 这个文件**实际上不依赖原项目内部包**，只依赖外部�?`github.com/golang-jwt/jwt/v5`。所以这一步对 `jwtx` 不需要修改源码�?
但如果是依赖原项目内部包的文件（比如某些 logic �?`import "go-zero-admin/pkg/xerr"`），那就**必须�?*�?
| 旧路�?                      | 新路�?                         |
| ---------------------------- | ------------------------------- |
| `go-zero-admin/pkg/jwtx`     | `go-zero-admin/common/jwtx`     |
| `go-zero-admin/pkg/xerr`     | `go-zero-admin/common/xerr`     |
| `go-zero-admin/pkg/constants`| `go-zero-admin/common/constants`|

#### 5.5.2 命令（如果有内部 import 需要批改）

GoLand �?"Replace in Path" 快捷�?`Ctrl+Shift+R`，搜索框填：
- 查找：`go-zero-admin/pkg/`
- 替换：`go-zero-admin/common/`
- 范围：仅�?`common/` 目录�?
或者在 Git Bash 里：

```bash
cd D:/GoProject/project_new/go-zero-admin/common
grep -rl "go-zero-admin/pkg/" . | xargs sed -i 's|go-zero-admin/pkg/|go-zero-admin/common/|g'
```

#### 5.5.3 当前 jwtx/xerr/constants 三个文件实际情况

我刚帮您查了，这三个文件�?*没有引用原项目内部包**，它们只引用了：

- `jwtx/jwt.go`: `github.com/golang-jwt/jwt/v5`（外部库�?- `xerr/errcode.go`: �?import `"fmt"`（标准库�?- `constants/role.go`: 完全�?import 任何东西

**所以这一步对当前三个文件无需任何修改�?* 但您要记住这条原则——后面搬 logic 文件时一定会用上�?
---

### 5.6 第六步：解决 IDE 报红——`go mod tidy` 下载依赖

> **关键知识点：为什么会�?"Cannot resolve symbol 'golang-jwt'"�?*
>
> 因为�?> 1. 您新建的 `common/go.mod` 是空的，没有声明任何 `require`
> 2. `jwt.go` �?`import "github.com/golang-jwt/jwt/v5"` 这个第三方库，本地根本没下载
> 3. IDE 顺着 `go.mod` 找不到这个包，于是标�?>
> 解决办法只有一行命令：`go mod tidy`。它会自动扫描所�?`.go` 文件�?import，把缺失的依赖写�?`go.mod` 并下载到本地缓存�?
#### 5.6.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/common

go mod tidy
```

#### 5.6.2 这条命令做什�?
按顺序执行四件事�?
1. 扫描 `common/` 下所�?`.go` 文件，收�?import 列表
2. 对每�?import�?   - 标准库（�?`fmt`, `errors`）→ 跳过
   - 已声明且使用的依�?�?保留
   - 未声明但被使用的 �?加到 `go.mod` �?`require` 段，并从网上下载
   - 已声明但没被使用�?�?�?`go.mod` 删除
3. 把所有依赖的精确版本和校验码写入 `go.sum`
4. 把第三方库源码下载到 `$GOPATH/pkg/mod/`（全局缓存，不会塞到您项目里）

#### 5.6.3 产物

`common/go.mod` 变成�?
```
module go-zero-admin/common

go 1.24.0

require github.com/golang-jwt/jwt/v5 v5.2.1
```

新增 `common/go.sum` 文件（自动生成的依赖校验码，**不要手动改它**）：

```
github.com/golang-jwt/jwt/v5 v5.2.1 h1:...
github.com/golang-jwt/jwt/v5 v5.2.1/go.mod h1:...
```

#### 5.6.4 验证

```bash
cat common/go.mod
# 应该看到 require github.com/golang-jwt/jwt/v5 已被加入
```

#### 5.6.5 IDE 还是报红怎么�?
`go mod tidy` 成功了，�?GoLand 仍然显示 "Cannot resolve symbol"——这�?**IDE 缓存问题**，不是代码问题。两步操作刷新缓存：

1. **GoLand**：File �?Invalidate Caches �?Invalidate and Restart
2. **VSCode**：Ctrl+Shift+P �?"Go: Restart Language Server"

或者更简单粗暴：关闭 IDE 重开�?
#### 5.6.6 `go mod tidy` 报错怎么�?
| 报错                                       | 原因               | 解决                                            |
| ------------------------------------------ | ------------------ | ----------------------------------------------- |
| `dial tcp: lookup proxy.golang.org`         | 网络问题           | 设置代理：`go env -w GOPROXY=https://goproxy.cn,direct` |
| `unknown revision v5.2.1`                  | 版本号写�?        | 删掉错的 require 行重�?tidy                    |
| `module ... was not found in latest`        | 模块名拼�?        | 检�?import 路径拼写                            |

---

### 5.7 第七步：初始�?sys-rpc 模块（先建空模块，后�?goctl 再填�?
#### 5.7.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc

go mod init go-zero-admin/sys-rpc
```

#### 5.7.2 这命令做什�?
跟第 5.3 步一样，�?sys-rpc 目录创建一�?`go.mod`，模块名�?`go-zero-admin/sys-rpc`�?
之所�?*现在就建�?module**，是为了下一步能�?`go.work` �?`use ./service/sys/rpc`——`go work use` 要求目标目录必须已经是个 Go 模块（有 `go.mod`）�?
#### 5.7.3 产物

`service/sys/rpc/go.mod`（内容仅一�?module + 一�?go 版本）�?
#### 5.7.4 后续

�?6 章会在这个模块里运行 `goctl rpc protoc` 生成代码—�?*先不要急着�?goctl，把�?5 章走完�?*

---

### 5.8 第八步：初始�?gateway 模块

#### 5.8.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/gateway

go mod init go-zero-admin/gateway
```

#### 5.8.2 产物

`gateway/go.mod`（同样是空模块壳）�?
---

### 5.9 第九步：�?go.work 把三个模块串起来

> **新手必读：什么是 Go Workspace�?*
>
> 您现在有 3 个独立模块（common、sys-rpc、gateway），它们彼此 import 时会有问题—�?> 比如 sys-rpc �?`import "go-zero-admin/common/jwtx"`，Go 默认会从远程拉取这个模块（因为它认为是网上的库）�?> �?`go-zero-admin/common` 根本没发布到 GitHub�?>
> 解决方案有两种：
> 1. **老办�?*：在每个模块�?`go.mod` 里加 `replace go-zero-admin/common => ../../../common`，繁琐易�?> 2. **新办法（Go 1.18+�?*：用 `go.work` 文件，告�?Go "这几个目录都在本地，直接用本地的，别去网上找"
>
> 我们用新办法�?
#### 5.9.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin

go work init
go work use ./common
go work use ./service/sys/rpc
go work use ./gateway
```

#### 5.9.2 这四条命令做什�?
- `go work init`：在根目录生�?`go.work` 文件
- `go work use ./common`：把 `./common` 这个本地模块登记�?`go.work` �?- 后面两行同理

#### 5.9.3 产物

`D:\GoProject\project_new\go-zero-admin\go.work` 文件�?
```
go 1.24.0

use (
    ./common
    ./service/sys/rpc
    ./gateway
)
```

#### 5.9.4 验证

跨模�?import 是否正常工作（这是后面真正要用的能力）�?*现在还没真实代码可验�?*，等�?8 章迁 logic 时会自动验证�?
#### 5.9.5 重要：什么时候不需�?go.work，什么时候需�?
| 场景                                | 需�?go.work�?|
| ----------------------------------- | -------------- |
| 单一模块项目（如您原项目�?         | 不需�?        |
| 多模块、本地相�?import（本次改造） | 必须需�?      |
| 部署到生�?                         | 不需要（部署时只编译单个模块�?|

`go.work` 是开发期工具�?*不要把它提交�?git**（建议加�?.gitignore，团队里每人本地维护）�?
---

### 5.10 第十步：复制 rbac_model.conf �?etc/

#### 5.10.1 命令

```bash
cp D:/GoProject/project_new/go-zero-rpc/etc/rbac_model.conf \
   D:/GoProject/project_new/go-zero-admin/etc/rbac_model.conf
```

#### 5.10.2 这是干什么的

`rbac_model.conf` �?Casbin 的权限模型定义文件，sys-rpc 启动时要读它。原项目里这个文件原样能用，复制即可�?
#### 5.10.3 原文件处�?
原项目的 `etc/rbac_model.conf` **保留不动**�?
---

### 5.11 �?5 章总验�?
到这一步，您的目录结构应该是：

```
D:\GoProject\project_new\go-zero-admin\
├── go.work                    <-- �?5.9 产物
├── common\
�?  ├── go.mod                 <-- �?5.3 产物
�?  ├── go.sum                 <-- �?5.6 产物
�?  ├── jwtx\jwt.go            <-- �?5.4 复制�?�?  ├── xerr\errcode.go
�?  └── constants\role.go
├── service\
�?  └── sys\
�?      └── rpc\
�?          └── go.mod         <-- �?5.7 产物
├── gateway\
�?  └── go.mod                 <-- �?5.8 产物
├── etc\
�?  └── rbac_model.conf        <-- �?5.10 复制�?└── deploy\
    (�?
```

每个模块单独 `go build ./...` 都应该通过�?
```bash
cd D:/GoProject/project_new/go-zero-admin/common
go build ./...
# 输出：（无输�?= 成功�?
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc
go build ./...
# 输出：no Go files in ...（正常，因为还没代码�?
cd D:/GoProject/project_new/go-zero-admin/gateway
go build ./...
# 输出：no Go files in ...（正常）
```

**至此里程�?1 完成**，您可以做一�?git commit�?scaffold: 创建 common/sys-rpc/gateway 三模块骨�?�?
---

### 5.12 �?5 章常见问题速查

| 问题                                            | 原因                              | 解决                                                                |
| ----------------------------------------------- | --------------------------------- | ------------------------------------------------------------------- |
| `go: command not found`                         | Go 没装或没�?PATH                | �?Go 1.24+，把 `<go-install>/bin` �?PATH                          |
| `go work init` �?`go: 'go.work' already exists`| 当前目录已有 go.work              | �?`rm go.work` 再重�?                                             |
| GoLand 红色波浪线一直在                         | IDE 缓存                          | File �?Invalidate Caches �?Invalidate and Restart                   |
| `go mod tidy` 卡住不动                          | 网络无法访问 proxy.golang.org     | `go env -w GOPROXY=https://goproxy.cn,direct`                       |
| `go.mod` 自动多了一堆奇�?require               | tidy 扫到了实验代码或测试文件     | 删掉那些不该 commit �?.go 文件，重�?tidy                          |
| 复制�?IDE 不识别新模块                         | go.work 没把它登�?               | `cd <根目�? && go work use ./<模块路径>`                            |
| `import "go-zero-admin/common/jwtx"` 报红       | go.work 缺失 / IDE 缓存 / tidy 没跑 | 三步排查�?) �?go.work�?) �?go mod tidy�?) 重启 IDE              |

---

## �?6 �?里程�?2：编�?sys.proto 并生�?RPC 骨架

> **本章前置**
>
> 在动手前，确认第 5 章已完成�?> - `service/sys/rpc/go.mod` 已存在（5.7 步产物）
> - `go.work` 里已 `use ./service/sys/rpc`�?.9 步产物）
> - protoc + protoc-gen-go + protoc-gen-go-grpc + goctl 已装好（�?3 章）
> - etcd 已启动（`docker ps | grep etcd-dev`�?>
> **本章目标**：让 sys-rpc 这个空模块拥有完整的 gRPC 服务骨架，能 `go run` 启动监听 9100，且�?etcd 注册成功�?*不写业务逻辑**——业务逻辑是第 8 章的事�?
---

### 6.1 第一步：创建 proto 文件存放目录

#### 6.1.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc
mkdir pb
```

#### 6.1.2 这命令做什�?
�?sys-rpc 模块根目录下建一个空目录 `pb/`，专门用来放 `.proto` 文件�?
#### 6.1.3 产物

```
service/sys/rpc/
├── go.mod      <-- �?5.7 步产�?└── pb/         <-- 新建空目�?```

#### 6.1.4 原文件处�?
无（这是一个新建目录，不涉及原项目）�?
---

### 6.2 第二步：编写 pb/sys.proto

> **新手必读：为什么先只写 Login 一个方法？**
>
> 改造期间最大的恐惧�?全部都写完了再发现链路不�?。先写最小可运行版本（MVP），跑�?前端 �?gateway �?sys-rpc �?MySQL �?返回"完整链路，再回头一个个补方法�?>
> **�?8 章会跑通这�?Login 链路；第 9 章再扩展其他方法�?*

#### 6.2.1 命令

�?GoLand 或任意编辑器，在 `service/sys/rpc/pb/` 下新建文�?`sys.proto`，粘贴以下内容：

```protobuf
syntax = "proto3";

package sys;
option go_package = "./sys";

// ========== 公共响应 ==========
message Empty {}

message CommonResp {
  int64  code = 1;        // 业务码，0 表示成功
  string msg  = 2;
}

// ========== 登录认证 ==========
message LoginReq {
  string username = 1;
  string password = 2;
  string client_ip = 3;       // �?gateway 注入（取�?X-Forwarded-For�?  string user_agent = 4;      // �?gateway 注入
}

message LoginResp {
  string accessToken  = 1;
  string refreshToken = 2;
  int64  expiresIn    = 3;
  UserInfo userInfo   = 4;
}

message UserInfo {
  int64  userId   = 1;
  string username = 2;
  string nickname = 3;
  string email    = 4;
  string phone    = 5;
  string avatar   = 6;
  repeated string roles = 7;
  repeated MenuItem menus = 8;
}

message MenuItem {
  int64  id        = 1;
  int64  parentId  = 2;
  string menuName  = 3;
  int64  menuType  = 4;
  string path      = 5;
  string component = 6;
  string icon      = 7;
  int64  sort      = 8;
  string perms     = 9;
  int64  status    = 10;
  repeated MenuItem children = 11;
}

// ========== 服务定义 ==========
service Sys {
  rpc Login (LoginReq) returns (LoginResp);
  // 其他方法暂时不写，第 9 章里程碑 5 再补
}
```

#### 6.2.2 产物

```
service/sys/rpc/pb/sys.proto    <-- 新建文件，约 60 �?```

#### 6.2.3 原文件处�?
无。`.proto` 是全新的接口定义文件，原项目没有对应物（原项目用的是 `.api` 文件）�?
#### 6.2.4 编辑器对 `.proto` 报错怎么�?
GoLand 默认不识�?`.proto` 语法，可能整个文件标红�?*这不是真�?*，可以忽略。要消除红线�?
- GoLand：Settings �?Plugins �?�?"Protocol Buffers" �?安装官方插件 �?重启
- VSCode：装 "vscode-proto3" 插件

---

### 6.3 第三步：�?goctl 生成 RPC 骨架

> **新手必读：goctl rpc protoc 在干什么？**
>
> 它是一个组合命令，等价于：
> 1. 调用 `protoc` �?`.proto` 编译�?Go 类型（`sys.pb.go`、`sys_grpc.pb.go`�?> 2. 在你指定的目录下生成 go-zero 风格的服务骨架（main、config、svc、logic、server�?> 3. 生成 client 包（给别的服务调用本服务用的�?>
> **一句话：`.proto` 进，完整可运行的 Go 服务雏形出�?*

#### 6.3.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc

goctl rpc protoc pb/sys.proto \
  --go_out=. \
  --go-grpc_out=. \
  --zrpc_out=. \
  -m
```

> Windows cmd 不支�?`\` 续行，所有命令写一行：
> `goctl rpc protoc pb/sys.proto --go_out=. --go-grpc_out=. --zrpc_out=. -m`

#### 6.3.2 各参数含�?
| 参数               | 作用                                                 |
| ------------------ | ---------------------------------------------------- |
| `pb/sys.proto`     | 输入文件                                             |
| `--go_out=.`       | 在当前目录生�?`sys.pb.go`（message 类型定义�?       |
| `--go-grpc_out=.`  | 生成 `sys_grpc.pb.go`（gRPC 接口契约�?               |
| `--zrpc_out=.`     | 生成 go-zero 风格�?main+config+svc+logic+server 骨架 |
| `-m`               | 把每�?rpc 方法�?logic 拆成单独文件，便于维�?        |

#### 6.3.3 产物（这一步会一次性生�?10+ 个文件）

```
service/sys/rpc/
├── go.mod                      <-- �?5.7 已存�?├── pb/sys.proto                <-- �?6.2 已存�?├── etc/sys.yaml                <-- 新生成：配置文件骨架
├── internal/
�?  ├── config/config.go        <-- 新生成：配置 struct
�?  ├── logic/
�?  �?  └── loginlogic.go       <-- 新生成：业务逻辑空壳（你后面要填这个�?�?  ├── server/
�?  �?  └── sysserver.go        <-- 新生成：gRPC server 入口（不用改�?�?  └── svc/
�?      └── servicecontext.go   <-- 新生成：ServiceContext（你后面要扩充）
├── sys/
�?  ├── sys.pb.go               <-- 新生成：Go 类型定义（不要改�?�?  └── sys_grpc.pb.go          <-- 新生成：gRPC 契约（不要改�?├── sysclient/
�?  └── sys.go                  <-- 新生成：给别的服务调用的 client（不要改�?└── sys.go                      <-- 新生成：main 入口
```

#### 6.3.4 原文件处�?
无。这一步是纯生成新文件，不动原项目任何东西�?
#### 6.3.5 报错排查

| 报错                                                  | 原因                                  | 解决                                                     |
| ----------------------------------------------------- | ------------------------------------- | -------------------------------------------------------- |
| `protoc-gen-go: program not found`                    | `protoc-gen-go` 没装或不�?PATH       | `go install google.golang.org/protobuf/cmd/protoc-gen-go@latest` 然后确保 `$GOPATH/bin` �?PATH |
| `protoc-gen-go-grpc: program not found`               | 同上                                  | `go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest` |
| `goctl: unknown command "rpc"`                        | goctl 太旧                            | `go install github.com/zeromicro/go-zero/tools/goctl@latest` |
| `pb/sys.proto: File not found`                        | 当前目录不对                          | 确保 `pwd` �?`service/sys/rpc/`                         |
| `--go_opt=paths=source_relative` 相关报错             | proto �?`option go_package` 写错     | 检�?`.proto` �?`option go_package = "./sys";` 这行     |

---

### 6.4 第四步：修复模块名（重要�?
#### 6.4.1 当前状�?
�?5.7 步您已经执行�?`go mod init go-zero-admin/sys-rpc`，所�?`go.mod` 里的模块名应该已经是 `go-zero-admin/sys-rpc`�?
�?goctl 生成的代码里 import 路径可能�?`<生成时的默认 module �?/sys`，需要核对一下�?
#### 6.4.2 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc
cat go.mod
```

应该看到第一行是�?
```
module go-zero-admin/sys-rpc
```

#### 6.4.3 验证 import 路径正确

打开 `internal/logic/loginlogic.go`，看 import�?
```go
import (
    "context"

    "go-zero-admin/sys-rpc/internal/svc"   // <-- 应该是这个前缀
    "go-zero-admin/sys-rpc/sys"

    "github.com/zeromicro/go-zero/core/logx"
)
```

如果 import 前缀不是 `go-zero-admin/sys-rpc/...`，说�?goctl 生成时用了别�?module 名�?*�?GoLand 全局替换**，或�?
```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc

# 把所有错误前缀替换成正确的
grep -rl "<错误前缀>" . | xargs sed -i 's|<错误前缀>|go-zero-admin/sys-rpc|g'
```

#### 6.4.4 报错排查

修复 import 路径后，下载所有依赖：

```bash
go mod tidy
```

这会�?go-zero、grpc、protobuf 等所有第三方库自动加�?`go.mod`，并下载到本地。完成后 `go.mod` �?`require` 段会�?5-10 行�?
**如果 `go mod tidy` 报错，先�?5.6 节的故障表�?*

---

### 6.5 第五步：填写 etc/sys.yaml 配置

#### 6.5.1 当前状�?
goctl 生成�?`etc/sys.yaml` 里只有最少的 `Name`、`ListenOn`、`Etcd` 三段�?*业务相关配置都缺**。需要把 DB、Redis、Auth、Casbin 等配置补全（直接照搬原项�?`etc/config.yaml`）�?
#### 6.5.2 操作

直接覆盖 `etc/sys.yaml` 内容为：

```yaml
Name: sys.rpc
ListenOn: 0.0.0.0:9100
Etcd:
  Hosts:
    - 127.0.0.1:2379
  Key: sys.rpc

Auth:
  AccessSecret: "your-access-secret-key-change-in-production"
  AccessExpire: 1200
  RefreshExpire: 10080

DB:
  DataSource: "user:password@tcp(127.0.0.1:3306)/database?charset=utf8mb4&parseTime=True&loc=Local&timeout=10s"

CacheRedis:
  - Host: "127.0.0.1:6379"
    Pass: "<redis-password>"
    Type: node

BizRedis:
  Host: 127.0.0.1:6379
  Pass: <redis-password>
  DB: 10

CasbinModelPath: "etc/rbac_model.conf"
UploadPath: "uploads"

Log:
  Mode: console
  Level: debug
  Encoding: plain
  WithCaller: true
```

#### 6.5.3 �?rbac_model.conf 复制到本模块�?etc �?
> **关键**：上�?`CasbinModelPath: "etc/rbac_model.conf"` 是相对路径，相对�?sys-rpc 启动时的工作目录。所以这个文件必须放�?`service/sys/rpc/etc/` 下�?
```bash
cp D:/GoProject/project_new/go-zero-admin/etc/rbac_model.conf \
   D:/GoProject/project_new/go-zero-admin/service/sys/rpc/etc/rbac_model.conf
```

> 注意：第 5.10 步把它放到了 `go-zero-admin/etc/` 下（全局位置）。这里再复制一份到 sys-rpc 私有 etc/。第 7 章的 svc 初始化才能找到它�?
#### 6.5.4 �?5 章产物现�?
```
go-zero-admin/etc/rbac_model.conf                       <-- 全局副本（第 5.10�?go-zero-admin/service/sys/rpc/etc/rbac_model.conf       <-- sys-rpc 私有副本（本步）
go-zero-admin/service/sys/rpc/etc/sys.yaml              <-- �?6.3 生成 + 本步覆盖
```

---

### 6.6 第六步：先跑一遍空 sys-rpc 验证骨架

> **此时 sys-rpc 还没有数据库连接，没有真实业务逻辑**。但 goctl 生成的空�?logic 应该能让进程启动起来�?
#### 6.6.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc
go run sys.go -f etc/sys.yaml
```

#### 6.6.2 预期

如果一切顺利，终端会输出：

```
Starting rpc server at 0.0.0.0:9100...
```

> **此时此刻 sys-rpc 进程在前台占用着终端**——不要关掉。打开**新终�?*做下一步验证�?
#### 6.6.3 验证 etcd 注册成功

新开一个终端：

```bash
docker exec etcd-dev etcdctl get --prefix sys.rpc
```

应该看到一行类似：

```
sys.rpc/7587858090345459714
127.0.0.1:9100
```

key 的后缀（数字）�?etcd 自动生成的注�?ID，每次启动都不一样。value �?sys-rpc 的网络地址�?
#### 6.6.4 关闭 sys-rpc

回到运行 sys-rpc 的那个终端，�?`Ctrl+C` 停止�?
#### 6.6.5 报错排查

| 报错                                            | 原因                                | 解决                                                                |
| ----------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------- |
| `Casbin 初始化失败：open etc/rbac_model.conf`    | 文件不在期望位置                    | 6.5.3 步：�?rbac_model.conf 复制�?sys-rpc 私有 etc/                |
| `Redis 连接失败`                                 | Redis 地址/密码�?                  | 检�?sys.yaml �?BizRedis                                            |
| `Error 1045 (28000): Access denied for user`    | DB 用户�?密码�?                   | 检�?sys.yaml �?DB.DataSource                                       |
| `dial tcp 127.0.0.1:2379: connect: refused`      | etcd 没启�?                        | `docker start etcd-dev`                                              |
| `mysql: bad parameter`                          | DSN 里参数错                        | 确认 timeout=10s 之类参数语法                                        |
| `panic: <某个 NewXxxModel �?nil>`              | ServiceContext 还没填业�?Model     | **正常**——第 7 章会填。当前用�?ServiceContext 跑只为验�?etcd 注册     |

> **如果 6.6 步暂时跑不起来（比如 ServiceContext 还没改）**：可以先�?`internal/svc/servicecontext.go` 里的所有业务初始化注释掉，只保�?`Config` 字段，让进程能启起来。第 7 章再回来恢复完整 ServiceContext�?
---

### 6.7 �?6 章总验�?
到这步：

- `service/sys/rpc/` 下文件齐全（10+ 个生成的文件�?- `go.mod` �?require 包含 go-zero、grpc、protobuf
- `go run sys.go -f etc/sys.yaml` 能监�?9100
- etcd 中能查到 `sys.rpc/...` 注册信息

可以做一�?git commit�?scaffold: 生成 sys-rpc gRPC 骨架并通过空启动验�?�?
---

## �?7 �?里程�?3：迁�?model �?+ pkg 工具�?
> **本章前置**
>
> - �?6 �?sys-rpc 骨架已生成，能空启动
> - 原项�?`D:\GoProject\project_new\go-zero-rpc\internal\model\system\` 包含 12 �?sys_* 表的 model 文件（每张表 2 个文件，�?24 �?.go + 1 �?vars.go�?> - 原项�?`D:\GoProject\project_new\go-zero-rpc\pkg\` 下有 11 个工具包
>
> **本章目标**：把 model + 7 �?sys-rpc 私有 pkg 复制过来，修�?import 路径，完�?ServiceContext 装配，让 `go run sys.go` 能带着真实数据库连接成功启动�?>
> **不在本章做的�?*：业�?logic 还是空的（第 8 章迁 Login）�?
---

### 7.1 第一步：复制 model 目录

> **新手必读：为什�?model 不放�?common�?*
>
> common �?跨服务共�?的代码（jwtx 验证 token �?gateway �?sys-rpc 都用得着的）�?> �?sys_* �?12 张表只属�?系统管理"业务�?*只有 sys-rpc 该碰它们**——其他业务服务（如未来的 plating-rpc）不应该绕过 sys-rpc 直接�?sys_user 表，必须�?sys-rpc 暴露�?RPC 方法�?> 所�?model �?sys-rpc �?私有领地"，放�?`service/sys/rpc/internal/model/` 下�?
#### 7.1.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc

mkdir -p internal/model
cp -r ../../../../go-zero-rpc/internal/model/system/* internal/model/
```

> Git Bash / PowerShell 都支持上面的写法�?> Windows cmd 用：`xcopy /E /I ..\..\..\..\go-zero-rpc\internal\model\system internal\model`
> 或者最简单：�?Windows 文件管理器里�?`D:\GoProject\project_new\go-zero-rpc\internal\model\system\` 整个目录的内容（不是这个文件夹本身，而是它里面的 25 个文件）拖到 `D:\GoProject\project_new\go-zero-admin\service\sys\rpc\internal\model\`�?
#### 7.1.2 这命令做什�?
把原项目 `internal/model/system/` 下所有文�?*复制**（不是移动）到新项目 `internal/model/` 下。注意：

- 复制的是**文件**，不�?`system` 这个文件夹本�?- 所以新项目里会变成 `internal/model/sys_user_model.go`（直接平铺）�?*没有** `system` 子文件夹
- 这样后面 import 路径�?`go-zero-admin/sys-rpc/internal/model`（短一层）

#### 7.1.3 产物

```
service/sys/rpc/internal/model/
├── sys_api_model.go
├── sys_api_model_gen.go
├── sys_dict_data_model.go
├── sys_dict_data_model_gen.go
├── sys_dict_type_model.go
├── sys_dict_type_model_gen.go
├── sys_file_model.go
├── sys_file_model_gen.go
├── sys_login_log_model.go
├── sys_login_log_model_gen.go
├── sys_menu_model.go
├── sys_menu_model_gen.go
├── sys_oper_log_model.go
├── sys_oper_log_model_gen.go
├── sys_role_api_model.go
├── sys_role_api_model_gen.go
├── sys_role_menu_model.go
├── sys_role_menu_model_gen.go
├── sys_role_model.go
├── sys_role_model_gen.go
├── sys_user_model.go
├── sys_user_model_gen.go
├── sys_user_role_model.go
├── sys_user_role_model_gen.go
└── vars.go
```

�?25 个文件�?
#### 7.1.4 原文件处�?
**原项�?`D:\GoProject\project_new\go-zero-rpc\internal\model\system\` 全部保留，不删不改�?*

#### 7.1.5 修改 package 声明（关键步骤！�?
原文件第一行是 `package system`。但新项目里目录变成�?`internal/model/`（没�?`system` 子目录），所�?*包名应该改成 `package model`**——否�?Go 编译器会�?package name conflict with directory name"�?
操作�?
- GoLand：双�?`internal/model/` 目录里任意一�?.go 文件，看�?`package system` �?�?"Replace in Path"（Ctrl+Shift+R）：
  - 查找：`package system`
  - 替换：`package model`
  - 范围：仅 `internal/model/` 目录
  - 全部替换
- Git Bash�?
```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc/internal/model
sed -i 's/^package system$/package model/' *.go
```

#### 7.1.6 验证

```bash
grep -l "^package system" internal/model/*.go
# 应该没有任何输出（说明全替换完了�?
grep -l "^package model" internal/model/*.go
# 应该列出全部 25 个文�?```

---

### 7.2 第二步：修复 model 内部�?import 路径

#### 7.2.1 当前问题

打开 `internal/model/sys_user_model.go`，您会看到类似这样的 import�?
```go
import (
    ...
    "go-zero-admin/pkg/xerr"        // <-- 旧路径，要改
    "go-zero-admin/internal/common" // <-- 如果有，也要�?    ...
)
```

这些都指�?*原单体项目的内部�?*。新项目里这些包的位置变了�?
#### 7.2.2 当前 model 文件实际依赖的旧 import（请自检�?
�?GoLand，打开 `internal/model/` 下任一 .go 文件，看 import 区。如果出现以下旧路径，全部需要改�?
| 旧路�?                                 | 新路�?                                               |
| --------------------------------------- | ----------------------------------------------------- |
| `go-zero-admin/pkg/xerr`                | `go-zero-admin/common/xerr`                           |
| `go-zero-admin/pkg/jwtx`                | `go-zero-admin/common/jwtx`                           |
| `go-zero-admin/pkg/constants`           | `go-zero-admin/common/constants`                      |
| `go-zero-admin/pkg/dict`                | `go-zero-admin/sys-rpc/pkg/dict`                      |
| `go-zero-admin/pkg/encrypt`             | `go-zero-admin/sys-rpc/pkg/encrypt`                   |
| `go-zero-admin/pkg/orm`                 | `go-zero-admin/sys-rpc/pkg/orm`                       |
| `go-zero-admin/pkg/sqlx`                | `go-zero-admin/sys-rpc/pkg/sqlx`                      |
| `go-zero-admin/pkg/casbin`              | `go-zero-admin/sys-rpc/pkg/casbin`                    |
| `go-zero-admin/pkg/upload`              | `go-zero-admin/sys-rpc/pkg/upload`                    |
| `go-zero-admin/pkg/excel`               | `go-zero-admin/sys-rpc/pkg/excel`                     |
| `go-zero-admin/pkg/response`            | （留�?gateway，本步不需要）                           |
| `go-zero-admin/internal/common`         | `go-zero-admin/sys-rpc/internal/common`（第 8 章建�?|
| `go-zero-admin/internal/middleware`     | （改造时弃用，第 11 章在 gateway 重写�?              |

#### 7.2.3 命令（先不批改，等第 7.3 步把 pkg 复制完再统一替换�?
不要现在改。先记住有这件事，第 7.3 步把 pkg 复制完后，本章会有一个统一�?批量替换 import"操作�?
---

### 7.3 第三步：复制 pkg 工具�?sys-rpc 私有目录

> **注意**：jwtx、xerr、constants �?5.4 步已经放�?common 了，**这一步不要再复制它们**�?> 本步只复�?sys-rpc 专用"�?7 �?pkg�?
#### 7.3.1 命令

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc

mkdir pkg
cp -r ../../../../go-zero-rpc/pkg/casbin    pkg/
cp -r ../../../../go-zero-rpc/pkg/encrypt   pkg/
cp -r ../../../../go-zero-rpc/pkg/orm       pkg/
cp -r ../../../../go-zero-rpc/pkg/sqlx      pkg/
cp -r ../../../../go-zero-rpc/pkg/upload    pkg/
cp -r ../../../../go-zero-rpc/pkg/dict      pkg/
cp -r ../../../../go-zero-rpc/pkg/excel     pkg/
```

#### 7.3.2 这命令做什�?
按照"每个 pkg 一个目�?的方式，�?7 个工具包从原项目复制�?sys-rpc 的私�?pkg 目录�?
#### 7.3.3 产物

```
service/sys/rpc/pkg/
├── casbin/         <-- Casbin Enforcer 初始�?├── encrypt/        <-- bcrypt 密码加密
├── orm/            <-- GORM 初始�?├── sqlx/           <-- sqlx 超时包装�?├── upload/         <-- 文件上传工具（注意：�?13 章可能搬�?gateway�?├── dict/           <-- 字典工具
└── excel/          <-- Excel 导出
```

每个目录里是�?pkg 下的 .go 文件，文件内容暂时不变�?
#### 7.3.4 原文件处�?
**原项�?`D:\GoProject\project_new\go-zero-rpc\pkg\` 下所有文件全部保留，不删不改�?*

#### 7.3.5 没有复制 jwtx / xerr / constants / response 的原�?
| 包名         | 改造去�?                 | 原因                                                          |
| ------------ | ------------------------- | ------------------------------------------------------------- |
| `jwtx`       | `common/jwtx`（第 5.4 已做）| gateway 验证 token，sys-rpc 生成 token，两边都用，所以放 common |
| `xerr`       | `common/xerr`（第 5.4 已做）| 错误码常量在两边都用                                          |
| `constants`  | `common/constants`（第 5.4 已做）| 角色编码等常量两边都�?                                 |
| `response`   | gateway（第 10 章再处理�?| 只有 HTTP 才需要响应封装，gRPC �?status.Errorf               |

---

### 7.4 第四步：批量修复 import 路径（关键步骤）

> **现在做之前在 7.2.3 推迟的事**：把所有旧 import 路径统一替换�?
#### 7.4.1 GoLand 操作

`Ctrl + Shift + R`（Replace in Path）打开全局替换�?
| 第几�?| 查找                                  | 替换                                          | 范围                                |
| :----: | ------------------------------------- | --------------------------------------------- | ----------------------------------- |
| 1      | `"go-zero-admin/pkg/xerr"`            | `"go-zero-admin/common/xerr"`                 | `service/sys/rpc/`                  |
| 2      | `"go-zero-admin/pkg/jwtx"`            | `"go-zero-admin/common/jwtx"`                 | `service/sys/rpc/`                  |
| 3      | `"go-zero-admin/pkg/constants"`       | `"go-zero-admin/common/constants"`            | `service/sys/rpc/`                  |
| 4      | `"go-zero-admin/pkg/`                  | `"go-zero-admin/sys-rpc/pkg/`                 | `service/sys/rpc/`                  |
| 5      | `"go-zero-admin/internal/model/system"` | `"go-zero-admin/sys-rpc/internal/model"`     | `service/sys/rpc/`                  |
| 6      | `"go-zero-admin/internal/`             | `"go-zero-admin/sys-rpc/internal/`            | `service/sys/rpc/`                  |

注意�?4 次替换的顺序�?*�?3 次先�?jwtx/xerr/constants 替换�?common 路径**，第 4 次才用通配�?`pkg/` 兜底剩下的（casbin/encrypt/orm/sqlx/upload/dict/excel）。如果顺序反了，jwtx 会被错误改成 `sys-rpc/pkg/jwtx`�?
#### 7.4.2 命令行操作（如果不用 IDE�?
```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc

# 先把 common 三件套替�?grep -rl "go-zero-admin/pkg/xerr" . | xargs sed -i 's|go-zero-admin/pkg/xerr|go-zero-admin/common/xerr|g'
grep -rl "go-zero-admin/pkg/jwtx" . | xargs sed -i 's|go-zero-admin/pkg/jwtx|go-zero-admin/common/jwtx|g'
grep -rl "go-zero-admin/pkg/constants" . | xargs sed -i 's|go-zero-admin/pkg/constants|go-zero-admin/common/constants|g'

# 兜底剩下�?pkg
grep -rl "go-zero-admin/pkg/" . | xargs sed -i 's|go-zero-admin/pkg/|go-zero-admin/sys-rpc/pkg/|g'

# model 路径
grep -rl "go-zero-admin/internal/model/system" . | xargs sed -i 's|go-zero-admin/internal/model/system|go-zero-admin/sys-rpc/internal/model|g'
grep -rl "go-zero-admin/internal/" . | xargs sed -i 's|go-zero-admin/internal/|go-zero-admin/sys-rpc/internal/|g'
```

#### 7.4.3 验证

```bash
# 应该完全没有结果
grep -r "go-zero-admin/pkg/" service/sys/rpc/
grep -r "go-zero-admin/internal/" service/sys/rpc/
```

如果还有结果，说明有遗漏的旧 import，按 7.4.1 / 7.4.2 再来一次�?
#### 7.4.4 �?go mod tidy 下载所有新依赖

import 路径正确了，�?sys-rpc 模块�?`go.mod` 还不知道要依赖哪些第三方库（gorm、casbin、redis 等）。同 5.6 步：

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc
go mod tidy
```

完成�?`go.mod` �?require 段会自动塞进 10+ 个第三方库（gorm.io/gorm、github.com/casbin/casbin/v2、github.com/redis/go-redis/v9 等）�?
---

### 7.5 第五步：扩充 ServiceContext

#### 7.5.1 当前状�?
打开 `internal/svc/servicecontext.go`，goctl 生成的内容很简陋，类似�?
```go
package svc

import "go-zero-admin/sys-rpc/internal/config"

type ServiceContext struct {
    Config config.Config
}

func NewServiceContext(c config.Config) *ServiceContext {
    return &ServiceContext{
        Config: c,
    }
}
```

我们要把它扩充成"持有 12 �?model + Orm + Redis + Casbin Enforcer"的完整版�?
#### 7.5.2 操作

**完全覆盖** `internal/svc/servicecontext.go` 内容为：

```go
package svc

import (
    "context"
    "fmt"
    "time"

    "go-zero-admin/sys-rpc/internal/config"
    sysmodel "go-zero-admin/sys-rpc/internal/model"
    "go-zero-admin/sys-rpc/pkg/casbin"
    "go-zero-admin/sys-rpc/pkg/orm"
    pkgsqlx "go-zero-admin/sys-rpc/pkg/sqlx"

    casbinv2 "github.com/casbin/casbin/v2"
    "github.com/redis/go-redis/v9"
    "github.com/zeromicro/go-zero/core/logx"
    "github.com/zeromicro/go-zero/core/stores/sqlx"
    "gorm.io/gorm"
)

// ServiceContext sys-rpc 服务上下�?//
// 与单体版相比的关键变化：
// 1. 不再�?AuthMiddleware/CasbinMiddleware（HTTP 中间件，留在 gateway�?// 2. 不再有业�?Model（如 PlateXXXModel），由各业务 RPC 自己管理
// 3. Enforcer 仍保留：sys-rpc 暴露 CheckPermission 方法�?gateway 远程调用
type ServiceContext struct {
    Config   config.Config
    Orm      *gorm.DB
    RDB      *redis.Client
    Enforcer *casbinv2.Enforcer

    SysUserModel     sysmodel.SysUserModel
    SysRoleModel     sysmodel.SysRoleModel
    SysUserRoleModel sysmodel.SysUserRoleModel
    SysMenuModel     sysmodel.SysMenuModel
    SysRoleMenuModel sysmodel.SysRoleMenuModel
    SysApiModel      sysmodel.SysApiModel
    SysRoleApiModel  sysmodel.SysRoleApiModel
    SysDictTypeModel sysmodel.SysDictTypeModel
    SysDictDataModel sysmodel.SysDictDataModel
    SysLoginLogModel sysmodel.SysLoginLogModel
    SysOperLogModel  sysmodel.SysOperLogModel
    SysFileModel     sysmodel.SysFileModel
}

func NewServiceContext(c config.Config) *ServiceContext {
    rawConn := sqlx.NewMysql(c.DB.DataSource)
    conn := pkgsqlx.NewTimeoutConn(rawConn, 30*time.Second)

    db := orm.NewMysql(&orm.Config{
        DSN:         c.DB.DataSource,
        Active:      20,
        Idle:        10,
        IdleTimeout: time.Hour * 24,
    })

    rdb := redis.NewClient(&redis.Options{
        Addr:        c.BizRedis.Host,
        Password:    c.BizRedis.Pass,
        DB:          c.BizRedis.DB,
        DialTimeout: 5 * time.Second,
        ReadTimeout: 3 * time.Second,
    })
    ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
    defer cancel()
    if err := rdb.Ping(ctx).Err(); err != nil {
        panic(fmt.Sprintf("Redis 连接失败�?v", err))
    }
    logx.Info("Redis 连接初始化成�?)

    enforcer, err := casbin.NewCasbin(db, c.CasbinModelPath)
    if err != nil {
        panic(fmt.Sprintf("Casbin 初始化失败：%v", err))
    }

    return &ServiceContext{
        Config:           c,
        Orm:              db,
        RDB:              rdb,
        Enforcer:         enforcer,
        SysUserModel:     sysmodel.NewSysUserModel(conn, c.CacheRedis, db),
        SysRoleModel:     sysmodel.NewSysRoleModel(conn, c.CacheRedis, db),
        SysUserRoleModel: sysmodel.NewSysUserRoleModel(conn, c.CacheRedis, db),
        SysMenuModel:     sysmodel.NewSysMenuModel(conn, c.CacheRedis, db),
        SysRoleMenuModel: sysmodel.NewSysRoleMenuModel(conn, c.CacheRedis, db),
        SysApiModel:      sysmodel.NewSysApiModel(conn, c.CacheRedis, db),
        SysRoleApiModel:  sysmodel.NewSysRoleApiModel(conn, c.CacheRedis, db),
        SysDictTypeModel: sysmodel.NewSysDictTypeModel(conn, c.CacheRedis, db),
        SysDictDataModel: sysmodel.NewSysDictDataModel(conn, c.CacheRedis, db),
        SysLoginLogModel: sysmodel.NewSysLoginLogModel(conn, c.CacheRedis, db),
        SysOperLogModel:  sysmodel.NewSysOperLogModel(conn, c.CacheRedis, db),
        SysFileModel:     sysmodel.NewSysFileModel(conn, c.CacheRedis, db),
    }
}
```

#### 7.5.3 这文件做什�?
- 收集 sys-rpc 启动时需要的所有共享资源（DB 连接、Redis 客户端、Casbin Enforcer�?- �?12 �?sys_* 表的 Model 实例�?- 后面所�?logic 都通过 `l.svcCtx.SysUserModel.XXX()` 访问 Model

#### 7.5.4 产物

`internal/svc/servicecontext.go` 内容被覆盖为完整版�?
#### 7.5.5 原文件处�?
- 原项�?`internal/svc/service_context.go` 不动
- 新项目里 goctl 生成的简陋版被覆盖（无需保留�?
---

### 7.6 第六步：扩充 Config struct

#### 7.6.1 当前状�?
`internal/config/config.go` 默认只有 `zrpc.RpcServerConf`�?
#### 7.6.2 操作

**完全覆盖** `internal/config/config.go` 内容�?
```go
package config

import (
    "github.com/zeromicro/go-zero/core/stores/cache"
    "github.com/zeromicro/go-zero/zrpc"
)

type Config struct {
    zrpc.RpcServerConf

    Auth struct {
        AccessSecret  string
        AccessExpire  int64
        RefreshExpire int64
    }

    DB struct {
        DataSource string
    }

    CacheRedis cache.CacheConf

    BizRedis struct {
        Host string
        Pass string
        DB   int
    }

    CasbinModelPath string
    UploadPath      string
}
```

#### 7.6.3 产物

`internal/config/config.go` 内容被覆盖�?
#### 7.6.4 原文件处�?
不动原项�?`internal/config/config.go`�?
---

### 7.7 第七步：再跑一�?go mod tidy

刚才 ServiceContext 又引入了新的 import（`gorm.io/gorm`、`github.com/casbin/casbin/v2` 等），需要再次同步依赖�?
```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc
go mod tidy
```

---

### 7.8 �?7 章总验�?
#### 7.8.1 编译验证

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc
go build ./...
```

期望：无任何输出（说明编译通过）�?
如果报错，按错误信息排查�?
| 报错示例                                         | 原因                              | 解决                                                 |
| ------------------------------------------------ | --------------------------------- | ---------------------------------------------------- |
| `cannot find package "go-zero-admin/common/..."` | go.work 没生�?/ IDE 缓存          | 检查根目录 go.work，重�?IDE                         |
| `package model: ... package system`              | 7.1.5 步漏�?package 声明          | 重新批量替换 `package system` �?`package model`      |
| `undefined: sysmodel.NewSysUserModel`            | model 文件缺失或包名不一�?        | 检�?internal/model/ 下所有文�?package 是否统一     |
| `cannot use ... as type ...`                     | 接口参数对不�?                    | 看具�?Model 的构造签名，�?ServiceContext 调用对齐  |

#### 7.8.2 运行验证

```bash
go run sys.go -f etc/sys.yaml
```

期望输出（前几行）：

```
sqlx MySQL 连接初始化成�?Redis 连接初始化成�?Casbin 执行器初始化成功
Starting rpc server at 0.0.0.0:9100...
```

新开终端验证 etcd 注册�?
```bash
docker exec etcd-dev etcdctl get --prefix sys.rpc
```

应该看到 sys.rpc 注册信息�?
#### 7.8.3 关闭进程

`Ctrl+C` 停止 sys-rpc�?
可以做一�?git commit�?feat(sys-rpc): 迁移 model + pkg + 完整 ServiceContext，通过启动验证"�?
---

### 7.9 �?7 章常见问题速查

| 现象                                                         | 原因                                                | 解决                                                                  |
| ------------------------------------------------------------ | --------------------------------------------------- | --------------------------------------------------------------------- |
| GoLand �?`internal/model/*.go` 显示 "found packages model and system" | 7.1.5 漏改了某些文件的 `package system`             | grep 一下还有几�?`package system` 没改                               |
| `import cycle not allowed`                                  | model 反向 import �?svc �?logic                   | 检�?model 文件，把这种反向引用注释掉，单体里有时为了便利会反向引用    |
| `gorm.io/driver/mysql: no required module`                  | go mod tidy 时网络断�?                             | `go env -w GOPROXY=https://goproxy.cn,direct` 后重�?tidy             |
| 启动 panic: `Casbin 初始化失败`                              | rbac_model.conf 找不�?                             | 复查 6.5.3 步：`service/sys/rpc/etc/rbac_model.conf` 必须存在         |
| 启动 panic: `Redis 连接失败`                                  | Redis 不�?                                         | `redis-cli -h 127.0.0.1 -p 6390 -a <redis-password>` 测连通�?          |
| 启动 panic: `dial tcp ...3307`                              | 数据库不�?                                         | 检�?MySQL 服务、网络、用户密�?                                       |

---

`service/sys/rpc/internal/svc/service_context.go`�?
```go
package svc

import (
    "context"
    "fmt"
    "time"

    "go-zero-admin/sys-rpc/internal/config"
    sysmodel "go-zero-admin/sys-rpc/internal/model"
    "go-zero-admin/sys-rpc/pkg/casbin"
    "go-zero-admin/sys-rpc/pkg/orm"
    pkgsqlx "go-zero-admin/sys-rpc/pkg/sqlx"

    casbinv2 "github.com/casbin/casbin/v2"
    "github.com/redis/go-redis/v9"
    "github.com/zeromicro/go-zero/core/logx"
    "github.com/zeromicro/go-zero/core/stores/sqlx"
    "gorm.io/gorm"
)

// ServiceContext sys-rpc 的服务上下文
//
// 与单体版相比的关键变化：
// 1. 不再�?AuthMiddleware/CasbinMiddleware（这些是 HTTP 中间件，留在 gateway�?// 2. 不再有任何业务相关的 Model（如 PlateXXXModel），那些是其�?RPC 的事
// 3. Enforcer 仍保留：sys-rpc 暴露 CheckPermission 方法�?gateway 远程调用
type ServiceContext struct {
    Config   config.Config
    Orm      *gorm.DB
    RDB      *redis.Client
    Enforcer *casbinv2.Enforcer

    SysUserModel     sysmodel.SysUserModel
    SysRoleModel     sysmodel.SysRoleModel
    SysUserRoleModel sysmodel.SysUserRoleModel
    SysMenuModel     sysmodel.SysMenuModel
    SysRoleMenuModel sysmodel.SysRoleMenuModel
    SysApiModel      sysmodel.SysApiModel
    SysRoleApiModel  sysmodel.SysRoleApiModel
    SysDictTypeModel sysmodel.SysDictTypeModel
    SysDictDataModel sysmodel.SysDictDataModel
    SysLoginLogModel sysmodel.SysLoginLogModel
    SysOperLogModel  sysmodel.SysOperLogModel
    SysFileModel     sysmodel.SysFileModel
}

func NewServiceContext(c config.Config) *ServiceContext {
    rawConn := sqlx.NewMysql(c.DB.DataSource)
    conn := pkgsqlx.NewTimeoutConn(rawConn, 30*time.Second)

    db := orm.NewMysql(&orm.Config{
        DSN:         c.DB.DataSource,
        Active:      20,
        Idle:        10,
        IdleTimeout: time.Hour * 24,
    })

    rdb := redis.NewClient(&redis.Options{
        Addr:        c.BizRedis.Host,
        Password:    c.BizRedis.Pass,
        DB:          c.BizRedis.DB,
        DialTimeout: 5 * time.Second,
        ReadTimeout: 3 * time.Second,
    })
    ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
    defer cancel()
    if err := rdb.Ping(ctx).Err(); err != nil {
        panic(fmt.Sprintf("Redis 连接失败�?v", err))
    }
    logx.Info("Redis 连接初始化成�?)

    enforcer, err := casbin.NewCasbin(db, c.CasbinModelPath)
    if err != nil {
        panic(fmt.Sprintf("Casbin 初始化失败：%v", err))
    }

    return &ServiceContext{
        Config:           c,
        Orm:              db,
        RDB:              rdb,
        Enforcer:         enforcer,
        SysUserModel:     sysmodel.NewSysUserModel(conn, c.CacheRedis, db),
        SysRoleModel:     sysmodel.NewSysRoleModel(conn, c.CacheRedis, db),
        SysUserRoleModel: sysmodel.NewSysUserRoleModel(conn, c.CacheRedis, db),
        SysMenuModel:     sysmodel.NewSysMenuModel(conn, c.CacheRedis, db),
        SysRoleMenuModel: sysmodel.NewSysRoleMenuModel(conn, c.CacheRedis, db),
        SysApiModel:      sysmodel.NewSysApiModel(conn, c.CacheRedis, db),
        SysRoleApiModel:  sysmodel.NewSysRoleApiModel(conn, c.CacheRedis, db),
        SysDictTypeModel: sysmodel.NewSysDictTypeModel(conn, c.CacheRedis, db),
        SysDictDataModel: sysmodel.NewSysDictDataModel(conn, c.CacheRedis, db),
        SysLoginLogModel: sysmodel.NewSysLoginLogModel(conn, c.CacheRedis, db),
        SysOperLogModel:  sysmodel.NewSysOperLogModel(conn, c.CacheRedis, db),
        SysFileModel:     sysmodel.NewSysFileModel(conn, c.CacheRedis, db),
    }
}
```

### 7.4 扩充 Config struct

`service/sys/rpc/internal/config/config.go`�?
```go
package config

import (
    "github.com/zeromicro/go-zero/core/stores/cache"
    "github.com/zeromicro/go-zero/zrpc"
)

type Config struct {
    zrpc.RpcServerConf

    Auth struct {
        AccessSecret  string
        AccessExpire  int64
        RefreshExpire int64
    }

    DB struct {
        DataSource string
    }

    CacheRedis cache.CacheConf

    BizRedis struct {
        Host string
        Pass string
        DB   int
    }

    CasbinModelPath string
    UploadPath      string
}
```

完整�?`etc/sys.yaml`�?
```yaml
Name: sys.rpc
ListenOn: 0.0.0.0:9100
Etcd:
  Hosts:
    - 127.0.0.1:2379
  Key: sys.rpc

Auth:
  AccessSecret: "your-access-secret-key-change-in-production"
  AccessExpire: 1200
  RefreshExpire: 10080

DB:
  DataSource: "user:password@tcp(127.0.0.1:3306)/database?charset=utf8mb4&parseTime=True&loc=Local&timeout=10s"

CacheRedis:
  - Host: "127.0.0.1:6379"
    Pass: "<redis-password>"
    Type: node

BizRedis:
  Host: 127.0.0.1:6379
  Pass: <redis-password>
  DB: 10

CasbinModelPath: "etc/rbac_model.conf"
UploadPath: "uploads"

Log:
  Mode: console
  Level: debug
  Encoding: plain
```

把原项目�?`etc/rbac_model.conf` 复制过来�?
`go run sys.go -f etc/sys.yaml` 应该能正常启动（虽然 Login 还没业务逻辑）�?
---

## �?8 �?里程�?4：迁�?logic（以 Login 为例打通端到端�?
> **重要修订**：第 8 章到�?14 章请优先�?`docx/微服务改造_�?章起修订�?md` 执行�?>
> 原文�?8 章的 Login Handler 示例直接使用�?`httpx.ErrorCtx` / `httpx.OkJsonCtx`，没有继承原项目 `pkg/response` 的统一响应格式；同时也没有说明 `gateway/internal/logic` 的职责。修订版已经按原 `go-zero-api` �?handler/logic/middleware 结构重新细化�?
### 8.1 单体�?Login（你已有的代码）

文件：`internal/logic/auth/login_logic.go`，签名：

```go
func (l *LoginLogic) Login(req *types.LoginReq, r *http.Request) (resp *types.LoginResp, err error)
```

它接�?`*http.Request` 来取 IP �?UserAgent—�?*这部分必须改�?*，因�?RPC 服务收不�?HTTP 请求�?
### 8.2 改造原则：HTTP 上下文信息由 gateway 注入

�?`LoginReq` 里加 `client_ip` �?`user_agent` 字段（已经在�?6.2 节加了）。gateway �?handler 把这两个字段填进去再�?RPC�?
### 8.3 sys-rpc �?Login logic

`service/sys/rpc/internal/logic/login_logic.go`（goctl 已经生成空壳）：

```go
package logic

import (
    "context"
    "time"

    "go-zero-admin/common/constants"
    "go-zero-admin/common/xerr"
    "go-zero-admin/sys-rpc/internal/svc"
    sysmodel "go-zero-admin/sys-rpc/internal/model"
    "go-zero-admin/sys-rpc/pkg/encrypt"
    "go-zero-admin/sys-rpc/sys"

    "go-zero-admin/common/jwtx"

    "github.com/zeromicro/go-zero/core/logx"
)

type LoginLogic struct {
    ctx    context.Context
    svcCtx *svc.ServiceContext
    logx.Logger
}

func NewLoginLogic(ctx context.Context, svcCtx *svc.ServiceContext) *LoginLogic {
    return &LoginLogic{
        ctx:    ctx,
        svcCtx: svcCtx,
        Logger: logx.WithContext(ctx),
    }
}

// Login 处理用户登录
//
// 业务流程与单体版完全一致：
// 1. 按用户名查询用户（不存在 �?错误�?// 2. 校验账号状态（禁用 �?错误�?// 3. bcrypt 校验密码
// 4. 生成 JWT �?Token
// 5. 查询用户角色 + 菜单
// 6. 异步记录登录日志
// 7. 返回 LoginResp
func (l *LoginLogic) Login(in *sys.LoginReq) (*sys.LoginResp, error) {
    // 1. 查用�?    user, err := l.svcCtx.SysUserModel.FindOneByUsername(l.ctx, in.Username)
    if err != nil {
        if err == sysmodel.ErrNotFound {
            l.recordLoginLog(in.Username, 0, in.ClientIp, in.UserAgent, 0, "用户名或密码错误")
            return nil, xerr.NewCodeError(xerr.ErrPasswordWrong)
        }
        l.Errorf("查询用户失败: %v", err)
        return nil, xerr.NewCodeError(xerr.ErrInternal)
    }

    // 2. 状�?    if user.Status != 1 {
        l.recordLoginLog(in.Username, user.Id, in.ClientIp, in.UserAgent, 0, "账号已被禁用")
        return nil, xerr.NewCodeError(xerr.ErrAccountDisabled)
    }

    // 3. 密码
    if !encrypt.CheckPassword(in.Password, user.Password) {
        l.recordLoginLog(in.Username, user.Id, in.ClientIp, in.UserAgent, 0, "密码错误")
        return nil, xerr.NewCodeError(xerr.ErrPasswordWrong)
    }

    // 4. Token
    accessToken, err := jwtx.GenerateToken(user.Id, user.Username,
        l.svcCtx.Config.Auth.AccessSecret, l.svcCtx.Config.Auth.AccessExpire)
    if err != nil {
        return nil, xerr.NewCodeError(xerr.ErrInternal)
    }
    refreshToken, err := jwtx.GenerateRefreshToken(user.Id, user.Username,
        l.svcCtx.Config.Auth.AccessSecret, l.svcCtx.Config.Auth.RefreshExpire)
    if err != nil {
        return nil, xerr.NewCodeError(xerr.ErrInternal)
    }

    // 5. 角色 + 菜单（业务逻辑跟单体一模一样，省略，直接复制）
    roleCodes, menuTree := l.loadRolesAndMenus(user.Id)

    // 6. 登录日志
    l.recordLoginLog(in.Username, user.Id, in.ClientIp, in.UserAgent, 1, "登录成功")

    // 7. 组装返回（types.UserInfo 改成 sys.UserInfo�?    return &sys.LoginResp{
        AccessToken:  accessToken,
        RefreshToken: refreshToken,
        ExpiresIn:    l.svcCtx.Config.Auth.AccessExpire,
        UserInfo: &sys.UserInfo{
            UserId:   user.Id,
            Username: user.Username,
            Email:    user.Email,
            Phone:    user.Phone,
            Nickname: user.Nickname,
            Avatar:   user.Avatar,
            Roles:    roleCodes,
            Menus:    menuTree,  // []*sys.MenuItem
        },
    }, nil
}

// loadRolesAndMenus 把单体版的角�?菜单查询逻辑搬过来，菜单树改成返�?[]*sys.MenuItem
func (l *LoginLogic) loadRolesAndMenus(userId int64) ([]string, []*sys.MenuItem) {
    // 实现略，参考原 login_logic.go �?87-130 行，
    // �?common.BuildMenuTree 的返回类型改�?[]*sys.MenuItem
    // ...
    return nil, nil
}

func (l *LoginLogic) recordLoginLog(username string, userId int64, ip, ua string, status int64, msg string) {
    go func() {
        // 浏览�?操作系统识别逻辑直接复制原项�?internal/common/browser.go
        // ...
        loginLog := &sysmodel.SysLoginLog{
            UserId:    userId,
            Username:  username,
            Ip:        ip,
            Browser:   "",       // common.ExtractBrowser(ua)
            Os:        "",       // common.ExtractOS(ua)
            Status:    status,
            Msg:       msg,
            LoginTime: time.Now(),
        }
        l.svcCtx.SysLoginLogModel.Insert(context.Background(), loginLog)
    }()
}
```

**关键�?*�?
1. 业务流程跟原 `login_logic.go` 完全一�?2. 输入类型�?`*types.LoginReq` 变成 `*sys.LoginReq`（proto 生成的）
3. 输出类型�?`*types.LoginResp` 变成 `*sys.LoginResp`
4. `*http.Request` 不再传——改成在 `LoginReq` 里带 `ClientIp`、`UserAgent`
5. `common.BuildMenuTree` 函数要改造：原来返回 `[]types.MenuItem`，现在要返回 `[]*sys.MenuItem`�?*直接写两个版�?/ 写个泛型版本都行**，最简单的是把它放�?sys-rpc 里专门给 RPC 用�?
### 8.4 写一个最简 gateway 跑�?Login

#### 8.4.1 创建 gateway

```bash
mkdir -p D:/GoProject/project_new/go-zero-admin/gateway/api
cd D:/GoProject/project_new/go-zero-admin/gateway/api

# 写一个最简 .api 文件
```

文件 `gateway/api/gateway.api`（先只放 Login）：

```go
syntax = "v1"

type LoginReq {
    Username string `json:"username"`
    Password string `json:"password"`
}
type LoginResp {
    AccessToken  string `json:"accessToken"`
    RefreshToken string `json:"refreshToken"`
    ExpiresIn    int64  `json:"expiresIn"`
    UserInfo     UserInfo `json:"userInfo"`
}
type UserInfo {
    UserId   int64    `json:"userId"`
    Username string   `json:"username"`
    Nickname string   `json:"nickname"`
    Email    string   `json:"email"`
    Phone    string   `json:"phone"`
    Avatar   string   `json:"avatar"`
    Roles    []string `json:"roles"`
    Menus    []MenuItem `json:"menus"`
}
type MenuItem {
    Id        int64      `json:"id"`
    ParentId  int64      `json:"parentId"`
    MenuName  string     `json:"menuName"`
    MenuType  int64      `json:"menuType"`
    Path      string     `json:"path"`
    Component string     `json:"component"`
    Icon      string     `json:"icon"`
    Sort      int64      `json:"sort"`
    Perms     string     `json:"perms"`
    Status    int        `json:"status"`
    Children  []MenuItem `json:"children,optional"`
}

@server (
    prefix: /api/auth
)
service gateway-api {
    @handler LoginHandler
    post /login (LoginReq) returns (LoginResp)
}
```

#### 8.4.2 生成 gateway 骨架

```bash
cd D:/GoProject/project_new/go-zero-admin/gateway
goctl api go -api api/gateway.api -dir . --style gozero
go mod init go-zero-admin/gateway
go mod tidy
```

#### 8.4.3 �?ServiceContext，加�?sys-rpc 客户�?
`gateway/internal/svc/service_context.go`�?
```go
package svc

import (
    "go-zero-admin/gateway/internal/config"
    "go-zero-admin/sys-rpc/sysclient"

    "github.com/zeromicro/go-zero/zrpc"
)

type ServiceContext struct {
    Config config.Config
    SysRpc sysclient.Sys   // <-- gRPC 客户�?}

func NewServiceContext(c config.Config) *ServiceContext {
    return &ServiceContext{
        Config: c,
        SysRpc: sysclient.NewSys(zrpc.MustNewClient(c.SysRpc)),
    }
}
```

`gateway/internal/config/config.go`�?
```go
package config

import (
    "github.com/zeromicro/go-zero/rest"
    "github.com/zeromicro/go-zero/zrpc"
)

type Config struct {
    rest.RestConf
    Auth struct {
        AccessSecret string
        AccessExpire int64
    }
    SysRpc zrpc.RpcClientConf   // <-- 关键：通过 etcd 发现 sys-rpc
}
```

`gateway/etc/gateway.yaml`�?
```yaml
Name: gateway
Host: 0.0.0.0
Port: 8888
Mode: dev
Timeout: 30000

Auth:
  AccessSecret: "your-access-secret-key-change-in-production"
  AccessExpire: 1200

SysRpc:
  Etcd:
    Hosts:
      - 127.0.0.1:2379
    Key: sys.rpc           # 必须�?sys-rpc �?Etcd.Key 完全一�?```

#### 8.4.4 改�?Login Handler

`gateway/internal/handler/loginhandler.go`�?
```go
package handler

import (
    "net/http"

    "go-zero-admin/gateway/internal/svc"
    "go-zero-admin/gateway/internal/types"
    "go-zero-admin/sys-rpc/sys"

    "github.com/zeromicro/go-zero/rest/httpx"
)

func LoginHandler(svcCtx *svc.ServiceContext) http.HandlerFunc {
    return func(w http.ResponseWriter, r *http.Request) {
        var req types.LoginReq
        if err := httpx.Parse(r, &req); err != nil {
            httpx.ErrorCtx(r.Context(), w, err)
            return
        }

        // �?HTTP 上下文信息注入到 RPC 请求�?        rpcResp, err := svcCtx.SysRpc.Login(r.Context(), &sys.LoginReq{
            Username:  req.Username,
            Password:  req.Password,
            ClientIp:  getClientIP(r),
            UserAgent: r.UserAgent(),
        })
        if err != nil {
            httpx.ErrorCtx(r.Context(), w, err)
            return
        }

        // RPC Response �?HTTP Response（字段对拷）
        resp := convertLoginResp(rpcResp)
        httpx.OkJsonCtx(r.Context(), w, resp)
    }
}

func getClientIP(r *http.Request) string {
    // 复制 internal/middleware/auth_middleware.go �?GetClientIP
    if v := r.Header.Get("X-Forwarded-For"); v != "" {
        return v
    }
    if v := r.Header.Get("X-Real-IP"); v != "" {
        return v
    }
    return r.RemoteAddr
}

func convertLoginResp(in *sys.LoginResp) *types.LoginResp {
    // 字段平铺转换，注意菜单递归
    // ...
    return &types.LoginResp{
        AccessToken:  in.AccessToken,
        RefreshToken: in.RefreshToken,
        ExpiresIn:    in.ExpiresIn,
        // UserInfo: ... (递归处理 Menus)
    }
}
```

#### 8.4.5 启动顺序

打开 3 个终端：

```bash
# Terminal 1: etcd
docker start etcd-dev

# Terminal 2: sys-rpc
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc
go run sys.go -f etc/sys.yaml

# Terminal 3: gateway
cd D:/GoProject/project_new/go-zero-admin/gateway
go run gateway.go -f etc/gateway.yaml
```

#### 8.4.6 测试

```bash
curl -X POST http://localhost:8888/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

如果能拿�?token�?*恭喜，端到端跑通了�?* 这是整个改造最关键的一步�?
---

## �?9 �?里程�?5：迁移剩�?system logic

### 9.1 批量迁移策略

按这个顺序补 proto + logic（每个完成后 sys-rpc 重启，gateway 加对�?handler 测一下）�?
1. **认证�?*（auth）：RefreshToken、Logout、ChangePassword、GetCurrentUser
2. **用户�?*（user）：CreateUser、ListUser、GetUser、UpdateUser、DeleteUser、ResetPassword
3. **角色�?*（role）：7 个方�?4. **菜单�?*（menu）：5 个方�?5. **接口�?*（api）：5 个方�?6. **字典�?*（dict）：8 个方�?7. **日志�?*（log）：4 个方�?8. **文件�?*（file）：3 个方法（文件上传特殊处理见第 13 章）
9. **内部方法**：CheckPermission、GetUserById、GetRolesByUserId（给 gateway 中间件和其他 RPC 用）

### 9.2 单个方法迁移�?五步�?

�?ListUser 为例�?
**�?1 �?*：在 `pb/sys.proto` 里加 message + rpc�?
```protobuf
message ListUserReq {
  int64  page     = 1;
  int64  pageSize = 2;
  string keyword  = 3;
  int64  status   = 4;
  bool   hasStatus = 5;     // 区分"未传"�?传了 0"
}
message UserItem {
  int64    id        = 1;
  string   username  = 2;
  string   nickname  = 3;
  string   email     = 4;
  string   phone     = 5;
  int64    status    = 6;
  string   avatar    = 7;
  string   remark    = 8;
  repeated string roles = 9;
  string   createdAt = 10;
  string   updatedAt = 11;
}
message ListUserResp {
  int64    total = 1;
  repeated UserItem list = 2;
}

service Sys {
  rpc Login(LoginReq) returns (LoginResp);
  rpc ListUser(ListUserReq) returns (ListUserResp);   // <-- 新增
}
```

**�?2 �?*：重新生成代码：

```bash
cd D:/GoProject/project_new/go-zero-admin/service/sys/rpc
goctl rpc protoc pb/sys.proto --go_out=. --go-grpc_out=. --zrpc_out=. -m
```

goctl 会自动生�?`internal/logic/list_user_logic.go`（保留你已有的，只新增空壳）�?
**�?3 �?*：填 logic（直接复制原 `list_user_logic.go` 内容，改类型）：

```go
func (l *ListUserLogic) ListUser(in *sys.ListUserReq) (*sys.ListUserResp, error) {
    // 处理 status 的可选�?    var statusPtr *int
    if in.HasStatus {
        s := int(in.Status)
        statusPtr = &s
    }

    users, total, err := l.svcCtx.SysUserModel.List(l.ctx, int(in.Page), int(in.PageSize), in.Keyword, statusPtr)
    if err != nil {
        return nil, xerr.NewCodeError(xerr.ErrInternal)
    }

    // 后面跟原 logic 完全一样，只是 types.UserItem -> sys.UserItem
    list := make([]*sys.UserItem, 0, len(users))
    for _, user := range users {
        list = append(list, &sys.UserItem{
            Id:        user.Id,
            Username:  user.Username,
            // ...
        })
    }
    return &sys.ListUserResp{Total: total, List: list}, nil
}
```

**�?4 �?*：在 gateway `.api` 里加对应 HTTP 接口（直�?copy 原项�?`api/desc/system/user.api` 的相关定义）�?
**�?5 �?*：在 gateway handler 里写转发逻辑�?
```go
func ListUserHandler(svcCtx *svc.ServiceContext) http.HandlerFunc {
    return func(w http.ResponseWriter, r *http.Request) {
        var req types.ListUserReq
        if err := httpx.Parse(r, &req); err != nil {
            httpx.ErrorCtx(r.Context(), w, err)
            return
        }
        rpcReq := &sys.ListUserReq{
            Page:     int64(req.Page),
            PageSize: int64(req.PageSize),
            Keyword:  req.Keyword,
        }
        if req.Status != nil {
            rpcReq.Status = int64(*req.Status)
            rpcReq.HasStatus = true
        }
        resp, err := svcCtx.SysRpc.ListUser(r.Context(), rpcReq)
        if err != nil {
            httpx.ErrorCtx(r.Context(), w, err)
            return
        }
        httpx.OkJsonCtx(r.Context(), w, convertListUserResp(resp))
    }
}
```

### 9.3 减少重复代码：考虑用代码生�?
写到�?3 个接口你就会发现 90% 是模板代码�?*两个简化方�?*�?
**方案 A（推荐新手）**：忍一忍，全部手写。每个接口大�?5-10 行模板�?0 个接口约 300 行，1 个下午能搞完�?
**方案 B**：用 protoc-gen-go-zerorpc-gateway 之类的工具自动生�?gateway handler。学习成本较高，第一次改造不推荐�?
---

## �?10 �?里程�?6：搭�?Gateway

### 10.1 完整�?gateway.api

把原项目 `api/desc/auth.api` + `api/desc/system/*.api` 合并到一�?`gateway/api/gateway.api`�?
```go
syntax = "v1"

import (
    "auth.api"
    "system/user.api"
    "system/role.api"
    "system/menu.api"
    "system/api.api"
    "system/dict.api"
    "system/log.api"
    "system/file.api"
)
```

子文件直接复制原项目同名文件�?*保留所�?type 定义�?service �?*——这些就�?HTTP 协议定义，不需要改�?
### 10.2 重新生成 gateway 代码

```bash
cd D:/GoProject/project_new/go-zero-admin/gateway
goctl api go -api api/gateway.api -dir . --style gozero
```

goctl 会生成所�?handler 空壳�?*接下来你的工作是**：把每个空壳替换�?�?RPC + 转响�?两步�?
### 10.3 中间件名称对�?
原项�?.api 里写 `middleware: AuthMiddleware,CasbinMiddleware`，goctl 会要�?ServiceContext 提供同名字段。在 ServiceContext 里加�?
```go
type ServiceContext struct {
    Config           config.Config
    SysRpc           sysclient.Sys
    AuthMiddleware   rest.Middleware
    CasbinMiddleware rest.Middleware
}
```

具体实现见第 11 章�?
---

## �?11 �?里程�?7：迁移中间件（Auth + Casbin�?
### 11.1 AuthMiddleware（几乎不变）

把原 `internal/middleware/auth_middleware.go` 复制�?`gateway/internal/middleware/auth_middleware.go`，只�?import 路径（`go-zero-admin/pkg/jwtx` �?`go-zero-admin/common/jwtx`）�?
JWT 解析�?gateway 本地完成，不需要调 RPC�?
### 11.2 CasbinMiddleware（关键改造点�?
原版�?CasbinMiddleware 持有 Enforcer �?DB 连接，直接查 sys_user_role / sys_role 表�?
**改造后必须改成调用 sys-rpc �?CheckPermission 方法**，因为：

1. gateway 不应该直连数据库（违反微服务边界�?2. 多个业务 RPC 都需要鉴权时，CheckPermission 应该�?sys-rpc 暴露的统一能力

#### 11.2.1 �?sys.proto �?CheckPermission

```protobuf
message CheckPermissionReq {
  int64  userId  = 1;
  string path    = 2;     // /api/system/user
  string method  = 3;     // GET/POST/PUT/DELETE
}
message CheckPermissionResp {
  bool   allowed = 1;
  string reason  = 2;     // 失败原因（用户无角色、角色无权限等）
}

service Sys {
  // ...
  rpc CheckPermission(CheckPermissionReq) returns (CheckPermissionResp);
}
```

#### 11.2.2 sys-rpc 实现

`internal/logic/check_permission_logic.go`�?
```go
func (l *CheckPermissionLogic) CheckPermission(in *sys.CheckPermissionReq) (*sys.CheckPermissionResp, error) {
    // 复制�?casbin_middleware.go �?40-90 行的核心逻辑
    roleIds, err := l.svcCtx.SysUserRoleModel.GetRoleIdsByUserId(l.ctx, in.UserId)
    if err != nil {
        return &sys.CheckPermissionResp{Allowed: false, Reason: "查询角色失败"}, nil
    }
    if len(roleIds) == 0 {
        return &sys.CheckPermissionResp{Allowed: false, Reason: "用户无角�?}, nil
    }
    for _, rid := range roleIds {
        role, err := l.svcCtx.SysRoleModel.FindOneByRoleId(l.ctx, rid)
        if err != nil || role == nil {
            continue
        }
        ok, _ := l.svcCtx.Enforcer.Enforce(role.Code, in.Path, in.Method)
        if ok {
            return &sys.CheckPermissionResp{Allowed: true}, nil
        }
    }
    return &sys.CheckPermissionResp{Allowed: false, Reason: "无权�?}, nil
}
```

#### 11.2.3 gateway �?CasbinMiddleware

`gateway/internal/middleware/casbin_middleware.go`�?
```go
package middleware

import (
    "net/http"

    "go-zero-admin/common/response"
    "go-zero-admin/sys-rpc/sys"
    "go-zero-admin/sys-rpc/sysclient"
)

func NewCasbinMiddleware(rpc sysclient.Sys) func(http.HandlerFunc) http.HandlerFunc {
    return func(next http.HandlerFunc) http.HandlerFunc {
        return func(w http.ResponseWriter, r *http.Request) {
            userId := GetUserIdFromCtx(r.Context())
            if userId == 0 {
                response.FailUnauthorized(w, r)
                return
            }
            resp, err := rpc.CheckPermission(r.Context(), &sys.CheckPermissionReq{
                UserId: userId,
                Path:   r.URL.Path,
                Method: r.Method,
            })
            if err != nil || !resp.Allowed {
                response.FailForbidden(w, r)
                return
            }
            next.ServeHTTP(w, r)
        }
    }
}
```

#### 11.2.4 ServiceContext 注册

```go
func NewServiceContext(c config.Config) *ServiceContext {
    rpc := sysclient.NewSys(zrpc.MustNewClient(c.SysRpc))
    return &ServiceContext{
        Config:           c,
        SysRpc:           rpc,
        AuthMiddleware:   middleware.NewAuthMiddleware(c),
        CasbinMiddleware: middleware.NewCasbinMiddleware(rpc),
    }
}
```

### 11.3 性能考量：Casbin 远程调用的代�?
每个请求都走一�?RPC 看起来浪费。两种优化（**改造期不要�?*，先跑通再说）�?
1. gateway 本地�?LRU 缓存：`(userId, path, method) -> bool`，TTL 30 �?2. gateway 本地维护 Casbin Enforcer 副本，订�?etcd 上的"权限变更"事件做增量同�?
---

## �?12 �?里程�?8：操作日志的迁移（最容易踩坑的一步）

### 12.1 原版 OperLogMiddleware 做了什�?
它在 HTTP 层捕获请求路径、方法、入参、响应、操作人，写�?sys_oper_log 表�?*这是一�?HTTP 中间�?*�?
### 12.2 改造方�?
**方案 A（推荐）**：日志收集在 gateway 完成，写库通过�?sys-rpc�?
```
gateway OperLogMiddleware
    �?    ├─ 包装 http.ResponseWriter 抓响应体
    ├─ 解析 r.Body 抓请求体
    ├─ 调用 next.ServeHTTP
    └─ 完成后异步调 sys-rpc.WriteOperLog(ctx, &sys.WriteOperLogReq{...})
```

**优点**：gateway 能拿�?HTTP 完整信息（reqUrl、HTTP method、user-agent），sys-rpc 只负责写库�?
#### 12.2.1 �?sys.proto 加方�?
```protobuf
message WriteOperLogReq {
  string title       = 1;
  string operType    = 2;
  string method      = 3;     // 函数�?handler�?  string reqMethod   = 4;     // HTTP method
  int64  operUserId  = 5;
  string operName    = 6;
  string deptName    = 7;
  string reqUrl      = 8;
  string reqParam    = 9;     // JSON 字符�?  string respResult  = 10;    // JSON 字符�?  int64  status      = 11;
  string ip          = 12;
  int64  durationMs  = 13;
  // operTime �?server time，不�?}

service Sys {
  rpc WriteOperLog(WriteOperLogReq) returns (CommonResp);
}
```

#### 12.2.2 gateway 中间�?
复制�?`internal/middleware/operlogmiddleware.go` �?gateway，把"直接 Insert" 改成"�?sys-rpc.WriteOperLog"�?
### 12.3 为什么不�?sys-rpc �?gRPC interceptor 捕获�?
因为�?
- gRPC interceptor 拿不�?HTTP method、原�?URL（这些在 gateway 已经被翻译成 RPC 调用，丢失了�?- 操作人（OperName/DeptName）需要查用户信息，gateway 已经�?ctx 里拿到了 userId，比 RPC 端再查一次更高效

---

## �?13 �?里程�?9：文件上传的特殊处理

### 13.1 问题

`UploadFileHandler` �?`multipart/form-data`，包含二进制文件流。如果让 gateway 把整个文件读进内存再�?gRPC 传给 sys-rpc，会出现�?
1. 内存爆炸（大文件�?2. gRPC 默认 4MB message 上限
3. 浪费带宽（gateway �?rpc 多一跳）

### 13.2 推荐方案：gateway 直接处理文件

文件上传是个**例外**——gateway 直接落盘 + 调用 sys-rpc 仅写元数据：

```
浏览�?multipart/form-data
    �?    �?gateway UploadFileHandler
    �?    ├─ 校验文件类型/大小
    ├─ 落盘到本地或 OSS
    ├─ �?sys-rpc.RegisterFile(ctx, &sys.RegisterFileReq{
    �?      fileName, fileURL, fileSize, mimeType, uploaderId
    �?  })  // 仅写 sys_file �?    └─ 返回 fileId、fileURL
```

**前提�?gateway �?sys-rpc 共享同一个文件存�?*——简单的做法是用 OSS/MinIO，复杂点是部�?NFS�?
### 13.3 sys.proto

```protobuf
message RegisterFileReq {
  string fileName    = 1;
  string fileUrl     = 2;
  int64  fileSize    = 3;
  string mimeType    = 4;
  int64  uploaderId  = 5;
}
message RegisterFileResp {
  int64  fileId = 1;
}

service Sys {
  rpc RegisterFile(RegisterFileReq) returns (RegisterFileResp);
  rpc DeleteFile(DeleteFileReq) returns (CommonResp);
  rpc ListFile(ListFileReq) returns (ListFileResp);
}
```

文件存储工具 `pkg/upload/` 留在 gateway 即可�?
---

## �?14 �?里程�?10：端到端联调与验�?
### 14.1 启动顺序（生�?开发都一样）

```bash
# 1. etcd
docker start etcd-dev

# 2. sys-rpc (�?etcd 起来后启)
cd service/sys/rpc && go run sys.go -f etc/sys.yaml &

# 3. gateway (�?sys-rpc 注册�?etcd 后启)
cd gateway && go run gateway.go -f etc/gateway.yaml &
```

### 14.2 推荐使用 docker-compose 一键管�?
`deploy/docker-compose.yml`�?
```yaml
version: "3.8"
services:
  etcd:
    image: bitnami/etcd:latest
    ports:
      - "2379:2379"
    environment:
      - ALLOW_NONE_AUTHENTICATION=yes
      - ETCD_ADVERTISE_CLIENT_URLS=http://0.0.0.0:2379
  sys-rpc:
    build: ../service/sys/rpc
    ports:
      - "9100:9100"
    depends_on:
      - etcd
    volumes:
      - ../etc/rbac_model.conf:/app/etc/rbac_model.conf
  gateway:
    build: ../gateway
    ports:
      - "8888:8888"
    depends_on:
      - sys-rpc
```

### 14.3 回归测试清单

按原项目 `docx/api_test_cases.md`（如果存在）跑一遍：

- [ ] POST /api/auth/login 登录成功，返�?token
- [ ] POST /api/auth/refresh 刷新 token
- [ ] POST /api/auth/logout 登出（token 进黑名单�?- [ ] POST /api/auth/changePassword 改密�?- [ ] GET /api/auth/userInfo 获取当前用户
- [ ] 用户管理 CRUD�? 个接口）
- [ ] 角色管理 CRUD�? 个接口）
- [ ] 菜单管理 CRUD�? 个接口）
- [ ] 接口管理 CRUD�? 个接口）
- [ ] 字典管理 CRUD�? 个接口）
- [ ] 日志查询/清空�? 个接口）
- [ ] 文件上传/下载/删除�? 个接口）
- [ ] 鉴权失败返回 401/403
- [ ] 操作日志正确写入 sys_oper_log

### 14.4 性能基线

�?wrk �?ab 跑一�?ListUser，对比单体版�?
```bash
wrk -t4 -c50 -d30s -H "Authorization: Bearer xxx" \
  http://localhost:8888/api/system/user?page=1&pageSize=20
```

预期：QPS 比单体下�?10-20%（gRPC 一跳的代价），但绝对值仍�?> 500 QPS�?
---

## �?15 �?未来如何嵌入业务服务（以 plating-rpc 为例�?
这是改造的最终目的—�?*�?通用系统管理"成为基础�?*�?
### 15.1 新建 plating-rpc 的工作量

```bash
mkdir -p D:/GoProject/project_new/go-zero-admin/service/plating/rpc
cd D:/GoProject/project_new/go-zero-admin/service/plating/rpc

# 1. �?plating.proto（定义业务接口，�?sys.proto 完全独立�?# 2. goctl 生成骨架
# 3. 复制业务 model �?internal/model/
# 4. �?ServiceContext 加入 sys-rpc 客户�?# 5. �?gateway �?plating 相关 .api 文件
```

### 15.2 plating-rpc �?ServiceContext

```go
type ServiceContext struct {
    Config config.Config
    Orm    *gorm.DB

    // 业务 Model（独立于 sys-rpc�?    PlateTankConfigModel platemodel.PlateTankConfigModel
    PlateProductionEventModel platemodel.PlateProductionEventModel
    // ...

    // �?sys-rpc 拿用户信息、写操作日志
    SysRpc sysclient.Sys
}

func NewServiceContext(c config.Config) *ServiceContext {
    return &ServiceContext{
        Config: c,
        Orm:    orm.NewMysql(...),
        // ...
        SysRpc: sysclient.NewSys(zrpc.MustNewClient(c.SysRpc)),
    }
}
```

### 15.3 业务 logic 怎么�?sys-rpc

```go
func (l *CreateTankLogic) CreateTank(in *plating.CreateTankReq) (*plating.CommonResp, error) {
    // 业务正常流程：写 plate_tank_config �?    // ...

    // 需要操作人姓名时，�?sys-rpc
    user, err := l.svcCtx.SysRpc.GetUserById(l.ctx, &sys.GetUserByIdReq{
        UserId: in.OperatorId,
    })
    if err != nil {
        // 容错：拿不到用户也不影响主流�?        l.Errorf("查询用户失败: %v", err)
    } else {
        // �?user.Nickname 记录�?plate_tank_config.created_by
    }

    return &plating.CommonResp{Code: 0, Msg: "ok"}, nil
}
```

### 15.4 gateway 路由分发

gateway 根据 URL 前缀决定调哪�?RPC�?
```
/api/auth/*     -> sys-rpc
/api/system/*   -> sys-rpc
/api/plating/*  -> plating-rpc
/api/order/*    -> order-rpc (将来)
```

ServiceContext 持有所�?RPC 客户端：

```go
type ServiceContext struct {
    Config     config.Config
    SysRpc     sysclient.Sys
    PlatingRpc platingclient.Plating
    // OrderRpc orderclient.Order  // 将来�?}
```

### 15.5 嵌入 sys-rpc 的真正价�?
新业务项�?*不需要重�?*�?
- 用户管理
- 角色权限
- 菜单系统
- 字典管理
- 操作日志
- 文件上传

只需要：

1. �?sys-rpc 部署起来（一份就够，所有业务共用）
2. 在自己的业务 RPC 里通过 `sysclient.NewSys(...)` 持有客户�?3. 该查用户查用户，该校验权限调 CheckPermission

**这就是微服务化最大的好处：基础能力一次开发，永久复用�?*

---

## �?16 �?常见坑与排查方法

### 16.1 sys-rpc 启动了但 gateway 连不�?
**现象**：gateway 启动�?`rpc dial fail` �?`etcd resolver: target Sys`�?
**排查**�?
1. `docker exec etcd-dev etcdctl get --prefix sys.rpc` �?sys-rpc 是否注册成功
2. 检�?sys.yaml �?`Etcd.Key` �?gateway.yaml �?`SysRpc.Etcd.Key` 是否完全一�?3. 检�?ListenOn �?`0.0.0.0:9100` 不是 `127.0.0.1:9100`（容器场景）

### 16.2 proto 改了但生成代码没更新

`goctl rpc protoc` 默认不会覆盖已有�?logic 文件（你已填的业务代码）。如果你改了 LoginReq 的字段：

- `sys.pb.go` 会自动更新（强制覆盖�?- `login_logic.go` 不会�?
但你�?logic 代码可能编译失败（因为引用了已删�?改名的字段）——这是正常的，按编译错误改即可�?
### 16.3 proto3 �?optional 怎么处理

原项目大量用�?`*string`、`*int` 表示可选字段（比如 UpdateUserReq）。proto3 处理方式�?
**方案 1（最简单）**：用零值表�?未传"，业务上能区分的字段（status=0 是有效值）就加 `bool hasStatus` 标记�?
**方案 2（proto3.15+�?*：使�?`optional` 关键字：

```protobuf
message UpdateUserReq {
  int64  id = 1;
  optional string username = 2;     // 生成 *string 类型
  optional string nickname = 3;
}
```

生成�?Go 代码会用 `*string`，跟原项目一致�?*这是最优解**，但要确保你�?protoc 版本 �?3.15�?
### 16.4 中文乱码（gRPC error message�?
gRPC 错误消息�?`status.Errorf`，中文是 OK 的。但 gateway �?error 转给前端时，要确保用 utf-8�?
`xerr.CodeError` �?RPC 端用 `status.Error(codes.Code, e.Msg)` 包装�?
```go
import "google.golang.org/grpc/status"
import "google.golang.org/grpc/codes"

return nil, status.Error(codes.Code(xerr.ErrPasswordWrong), "用户名或密码错误")
```

gateway 端：

```go
if err != nil {
    if st, ok := status.FromError(err); ok {
        response.Fail(w, r, int32(st.Code()), st.Message())
        return
    }
    response.FailInternal(w, r)
    return
}
```

### 16.5 跨进程上下文丢失

userId 在单体里�?ctx.Value 传的。改造后�?
- gateway �?AuthMiddleware �?userId 写入 ctx
- gateway handler �?RPC 时，要把 userId 通过 RPC 参数显式�?- **gRPC ctx.Value 在跨进程时不会自动序列化**

如果 RPC 端需�?当前用户 ID"，方案：

- 显式�?proto Req 里加 `int64 callerUserId = 999;` 字段
- 或者用 gRPC metadata：`md := metadata.Pairs("user-id", strconv.FormatInt(userId, 10))`，gateway �?RPC 前注入，RPC 端用 interceptor 解出�?
### 16.6 数据库连接数爆炸

sys-rpc 持有一个连接池，每个并发请求占一个连接。如果业�?RPC 也直�?sys 的库，连接数翻倍�?
**解决**：sys �?*只让 sys-rpc �?*，其他业�?RPC 想拿用户数据必须�?sys-rpc �?API。这�?*微服务的边界纪律**，不要破坏�?
### 16.7 编译报错：找不到 sys �?
跨模块导入时，如果不�?`go.work`，必须在 gateway �?go.mod 加：

```
require go-zero-admin/sys-rpc v0.0.0
replace go-zero-admin/sys-rpc => ../service/sys/rpc
```

�?`go.work` 就不需�?`replace`�?
---

## 附录 A：完�?sys.proto 模板

以下是建议最终完整版（节选关键部分，按你原项目接口数 1:1 对应）：

```protobuf
syntax = "proto3";
package sys;
option go_package = "./sys";

// ============ 公共 ============
message Empty {}
message CommonResp {
  int64  code = 1;
  string msg  = 2;
}

// ============ 认证 ============
message LoginReq { string username = 1; string password = 2; string clientIp = 3; string userAgent = 4; }
message LoginResp { string accessToken = 1; string refreshToken = 2; int64 expiresIn = 3; UserInfo userInfo = 4; }
message RefreshTokenReq { string refreshToken = 1; }
message RefreshTokenResp { string accessToken = 1; int64 expiresIn = 2; }
message LogoutReq { string accessToken = 1; }
message ChangePasswordReq { int64 userId = 1; string oldPassword = 2; string newPassword = 3; }
message GetCurrentUserReq { int64 userId = 1; }
message GetCurrentUserResp { UserInfo userInfo = 1; }

// ============ 用户管理 ============
message UserItem { ... }     // 字段同前�?message CreateUserReq { ... }
message UpdateUserReq { ... }
message DeleteUserReq { int64 id = 1; }
message GetUserReq { int64 id = 1; }
message ListUserReq { int64 page = 1; int64 pageSize = 2; string keyword = 3; int64 status = 4; bool hasStatus = 5; }
message ListUserResp { int64 total = 1; repeated UserItem list = 2; }
message ResetPasswordReq { int64 id = 1; string newPassword = 2; }

// ============ 角色管理 ============
message RoleItem { ... }
message CreateRoleReq { ... }
message UpdateRoleReq { ... }
message UpdateRolePermissionsReq { int64 id = 1; repeated int64 menuIds = 2; repeated int64 apiIds = 3; }
message DeleteRoleReq { int64 id = 1; }
message GetRoleReq { int64 id = 1; }
message ListRoleReq { int64 page = 1; int64 pageSize = 2; string keyword = 3; }
message ListRoleResp { int64 total = 1; repeated RoleItem list = 2; }
message RoleOption { int64 id = 1; string roleName = 2; string roleCode = 3; }
message ListAllRoleResp { repeated RoleOption listAll = 1; }

// ============ 菜单管理 ============
message MenuItem { ... }
message CreateMenuReq { ... }
message UpdateMenuReq { ... }
message DeleteMenuReq { int64 id = 1; }
message GetMenuTreeReq { }
message GetMenuTreeResp { repeated MenuItem list = 1; }
message GetCurrentUserMenusReq { int64 userId = 1; }

// ============ API 接口管理 ============
message ApiItem { ... }
message CreateApiReq { ... }
message UpdateApiReq { ... }
message DeleteApiReq { int64 id = 1; }
message ListApiReq { int64 page = 1; int64 pageSize = 2; string keyword = 3; string group = 4; }
message ListApiResp { int64 total = 1; repeated ApiItem list = 2; }
message ApiOption { int64 id = 1; string apiName = 2; string apiPath = 3; string remark = 4; }
message ListAllApiResp { repeated ApiOption listAll = 1; }

// ============ 字典管理 ============
message DictTypeItem { ... }
message CreateDictTypeReq { ... }
message UpdateDictTypeReq { ... }
message DeleteDictTypeReq { int64 id = 1; }
message ListDictTypeReq { int64 page = 1; int64 pageSize = 2; string keyword = 3; }
message ListDictTypeResp { int64 total = 1; repeated DictTypeItem list = 2; }
message DictDataItem { ... }
message CreateDictDataReq { ... }
message UpdateDictDataReq { ... }
message DeleteDictDataReq { int64 id = 1; }
message GetDictDataByTypeReq { string dictType = 1; }
message ListDictDataResp { int64 total = 1; repeated DictDataItem list = 2; }

// ============ 日志管理 ============
message LoginLogItem { ... }
message ListLoginLogReq { ... }
message ListLoginLogResp { int64 total = 1; repeated LoginLogItem list = 2; }
message OperLogItem { ... }
message ListOperLogReq { ... }
message ListOperLogResp { int64 total = 1; repeated OperLogItem list = 2; }
message WriteOperLogReq { ... }   // �?12 �?
// ============ 文件管理 ============
message FileItem { ... }
message RegisterFileReq { ... }
message RegisterFileResp { int64 fileId = 1; string fileUrl = 2; }
message DeleteFileReq { int64 id = 1; }
message ListFileReq { int64 page = 1; int64 pageSize = 2; string keyword = 3; }
message ListFileResp { int64 total = 1; repeated FileItem list = 2; }

// ============ 内部方法（给其他 RPC 用）============
message GetUserByIdReq { int64 userId = 1; }
message CheckPermissionReq { int64 userId = 1; string path = 2; string method = 3; }
message CheckPermissionResp { bool allowed = 1; string reason = 2; }
message GetRolesByUserIdReq { int64 userId = 1; }
message GetRolesResp { repeated string roleCodes = 1; }

// ============ 服务定义 ============
service Sys {
  // 认证
  rpc Login(LoginReq) returns (LoginResp);
  rpc RefreshToken(RefreshTokenReq) returns (RefreshTokenResp);
  rpc Logout(LogoutReq) returns (CommonResp);
  rpc ChangePassword(ChangePasswordReq) returns (CommonResp);
  rpc GetCurrentUser(GetCurrentUserReq) returns (GetCurrentUserResp);

  // 用户
  rpc CreateUser(CreateUserReq) returns (CommonResp);
  rpc UpdateUser(UpdateUserReq) returns (CommonResp);
  rpc DeleteUser(DeleteUserReq) returns (CommonResp);
  rpc GetUser(GetUserReq) returns (UserItem);
  rpc ListUser(ListUserReq) returns (ListUserResp);
  rpc ResetPassword(ResetPasswordReq) returns (CommonResp);

  // 角色
  rpc CreateRole(CreateRoleReq) returns (CommonResp);
  rpc UpdateRole(UpdateRoleReq) returns (CommonResp);
  rpc UpdateRolePermissions(UpdateRolePermissionsReq) returns (CommonResp);
  rpc DeleteRole(DeleteRoleReq) returns (CommonResp);
  rpc GetRole(GetRoleReq) returns (RoleItem);
  rpc ListRole(ListRoleReq) returns (ListRoleResp);
  rpc ListAllRole(Empty) returns (ListAllRoleResp);

  // 菜单
  rpc CreateMenu(CreateMenuReq) returns (CommonResp);
  rpc UpdateMenu(UpdateMenuReq) returns (CommonResp);
  rpc DeleteMenu(DeleteMenuReq) returns (CommonResp);
  rpc GetMenuTree(GetMenuTreeReq) returns (GetMenuTreeResp);
  rpc GetCurrentUserMenus(GetCurrentUserMenusReq) returns (GetMenuTreeResp);

  // 接口
  rpc CreateApi(CreateApiReq) returns (CommonResp);
  rpc UpdateApi(UpdateApiReq) returns (CommonResp);
  rpc DeleteApi(DeleteApiReq) returns (CommonResp);
  rpc ListApi(ListApiReq) returns (ListApiResp);
  rpc ListAllApi(Empty) returns (ListAllApiResp);

  // 字典
  rpc CreateDictType(CreateDictTypeReq) returns (CommonResp);
  rpc UpdateDictType(UpdateDictTypeReq) returns (CommonResp);
  rpc DeleteDictType(DeleteDictTypeReq) returns (CommonResp);
  rpc ListDictType(ListDictTypeReq) returns (ListDictTypeResp);
  rpc CreateDictData(CreateDictDataReq) returns (CommonResp);
  rpc UpdateDictData(UpdateDictDataReq) returns (CommonResp);
  rpc DeleteDictData(DeleteDictDataReq) returns (CommonResp);
  rpc GetDictDataByType(GetDictDataByTypeReq) returns (ListDictDataResp);

  // 日志
  rpc ListLoginLog(ListLoginLogReq) returns (ListLoginLogResp);
  rpc ClearLoginLog(Empty) returns (CommonResp);
  rpc ListOperLog(ListOperLogReq) returns (ListOperLogResp);
  rpc ClearOperLog(Empty) returns (CommonResp);
  rpc WriteOperLog(WriteOperLogReq) returns (CommonResp);

  // 文件
  rpc RegisterFile(RegisterFileReq) returns (RegisterFileResp);
  rpc DeleteFile(DeleteFileReq) returns (CommonResp);
  rpc ListFile(ListFileReq) returns (ListFileResp);

  // 内部
  rpc GetUserById(GetUserByIdReq) returns (UserItem);
  rpc CheckPermission(CheckPermissionReq) returns (CheckPermissionResp);
  rpc GetRolesByUserId(GetRolesByUserIdReq) returns (GetRolesResp);
}
```

---

## 附录 B：检查清单（验收标准�?
### 项目结构

- [ ] `D:/GoProject/project_new/go-zero-admin/` 创建完成
- [ ] `go.work` 包含 common、sys-rpc、gateway 三个模块
- [ ] etcd 容器正常运行

### sys-rpc

- [ ] `pb/sys.proto` 包含 50+ �?message�?0+ �?rpc 方法
- [ ] `goctl rpc protoc` 无错�?- [ ] `etc/sys.yaml` 配置完整
- [ ] `sys.go` 能启动监�?:9100
- [ ] `etcdctl get --prefix sys.rpc` 能看到注册信�?- [ ] 每个 logic 文件业务逻辑都已填充
- [ ] `internal/model/` 12 �?model 文件完整
- [ ] `pkg/casbin`、`pkg/encrypt`、`pkg/orm`、`pkg/sqlx`、`pkg/upload` 复制完成

### gateway

- [ ] `api/gateway.api` 包含所�?HTTP 接口定义
- [ ] `goctl api go` 无错�?- [ ] `etc/gateway.yaml` 配置 SysRpc.Etcd
- [ ] AuthMiddleware 能验�?JWT
- [ ] CasbinMiddleware 通过 RPC 调用鉴权
- [ ] 30+ �?handler 全部实现 RPC 转发
- [ ] 文件上传走本地落�?+ RPC 写元数据
- [ ] OperLog 中间件异步调 RPC 写日�?
### 端到�?
- [ ] curl 登录�?token 成功
- [ ] �?token �?`/api/system/user` 返回用户列表
- [ ] 不带 token �?`/api/system/user` 返回 401
- [ ] 用低权限用户调高权限接口返回 403
- [ ] 操作日志能在 sys_oper_log 看到
- [ ] 文件上传/下载/删除全流程�?
### 文档

- [ ] README 写明启动顺序
- [ ] docker-compose.yml 一键启动开发环�?- [ ] CHANGELOG 标注从单体迁移的版本

---

## 写在最�?
**改造的本质**�?�?函数调用"改成"网络调用"。代�?90% 不变，关键是理解�?
1. 进程边界在哪（gateway 不直接连数据�?/ sys-rpc 不直接处�?HTTP�?2. 什么数据要跨进程显式传（userId、ClientIP、UserAgent�?3. 哪些操作要变�?RPC 调用（CheckPermission、GetUserById、WriteOperLog�?
**给你的建�?*�?- �?1 周只追求"Login + ListUser 端到端�?——里程碑 1-4 全部完成
- �?2 周批量平移其他接口——里程碑 5-6
- �?3 周补中间件、操作日志、文件上传——里程碑 7-9
- �?4 周联调测试——里程碑 10

**遇到问题的求助方�?*�?- 编译错误：贴出报错完整堆栈和相关 import
- 运行错误：贴出日�?+ etcd 注册情况 + 配置文件
- 业务逻辑没头绪：对照�?logic 文件看，**90% 情况下原逻辑能直接复�?*

祝改造顺利�?